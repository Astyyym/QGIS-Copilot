# QGIS Copilot 开发短计划

> 版本：v2.0
> 状态：可供 Goal 模式直接执行
> 产品：QGIS Copilot
> 本轮目标：将已完成的安全 MVP 升级为“可信 GIS 工作台”，优先补足数据检查与分析，并以受控空间处理承接诊断结论
> 权威项目目录：`D:\wenjian\Hermes\QGIS Copilot`
> 前置基线：原 Goal 1–5 已完成；其实现与验收事实保留在 `task_plan.md`、`findings.md` 与 Git 历史中。本计划只定义后续增量 Goal，不重做已完成能力。

## 执行原则

- 先实现可解释、可确认、可追溯的工作台体验，再扩展工具；不做工具数量竞赛。
- 本轮主任务链为：`看懂数据 → 发现问题 → 明确目标 → 形成方案 → 确认执行 → 验证结果 → 继续迭代`。
- 数据检查与分析、受控空间处理按阶段交替闭环：每补一类诊断能力，都优先补一项能安全承接诊断结论的处理能力。
- 每个 Goal 必须独立验收；阶段门未通过不得进入下一 Goal 或扩展范围。
- 按职责拆分模块，不将 UI、模型请求、Agent、PyQGIS 调用、Processing 和计划执行堆进单个文件。
- 保持 Python + qgis.PyQt Qt Widgets 单体插件边界；不引入 TypeScript、Node.js、React、WebEngine、MCP 或独立后端。
- 网络请求不得阻塞 QGIS；所有 PyQGIS 与地图画布/UI 操作必须回到 QGIS 主线程；Processing 使用 QGIS 任务机制。
- 模型能力必须基于真实的能力档案和请求参数；不提供无实际效果的“思考强度”开关，不显示原始思维链或虚假百分比进度。
- 每完成一个 Goal，立即运行 QGIS 自带 Python 自动化和真实 Desktop 用户路径，并把结果更新至 `task_plan.md`、`findings.md`、README/架构文档（若范围或使用方式受影响）。
- 不用模拟响应、静态假数据、截图猜测或“代码看起来完成”替代真实 QGIS 运行证据。

## Goal 模式执行合同

### 总目标

在现有 QGIS Copilot MVP 上交付一个可理解当前会话、可解释工具轨迹、可诊断项目数据、可安全执行高频矢量处理并验证结果的原生 QGIS 工作台。用户无需安装 Hermes、Node.js、uv、MCP Client 或独立服务。

### 允许修改的范围

- `qgis_copilot/` 中为本轮 UI、模型能力档案、Agent 事件、只读工具、执行计划、Processing 任务和结果动作新增或修改的正式代码；
- `tests/`、`scripts/` 中与本轮能力直接相关的测试与验证脚本；
- 项目文档：`产品需求文档.md`、`架构说明.md`、`README.md`、`开发短计划.md`、`task_plan.md`、`findings.md`；
- `references/QGIS-4.0-MCP-public/` 仅供阅读，禁止修改；
- 测试数据必须使用虚构、公开或临时小型数据，不得将哥哥真实项目数据、API Key 或敏感路径纳入仓库、测试快照或日志。

### 停止条件

遇到以下情况必须停止当前 Goal，记录真实原因并由哥哥决定：

- 需要改变 Python 单体插件、原生 Qt Widgets 或内部 Tool Registry 的已确定架构；
- 需要引入 Node.js、TypeScript、React、WebEngine、MCP、独立服务或新的运行时依赖；
- 需要上传真实图层、项目、API Key 或不受控的完整属性数据；
- 需要开放任意 PyQGIS 代码执行、自动执行模式、直接改源数据、删除字段/要素、覆盖文件或自动保存项目；
- 需要改动 `references/QGIS-4.0-MCP-public/`；
- 目标 QGIS 无法启动，导致真实 Desktop 验收不能完成；
- 需求、权限边界、数据发送边界、交付范围或安全模型发生变化。

普通实现细节不确定时，应先查现有代码、QGIS 官方文档、Processing 参数与实际运行时；不要把可查问题升级成阻塞。

### 完成条件

一个 Goal 只有在以下条件都满足时才算完成：

- 该 Goal 的验收标准全部通过；
- QGIS 自带 Python 的相关自动化测试 exit 0；
- 真实 QGIS Desktop 路径已验证，或明确记录为阻塞/部分完成而不宣称通过；
- 结果不破坏已完成 MVP 的模型安全、只读边界、计划确认和输出验证；
- `task_plan.md`、`findings.md` 与受影响的用户文档已同步；
- 工作区没有未说明的调试残留、敏感数据或临时输出。

## 本轮目标目录结构

在既有结构上按真实职责增加模块；不要创建空壳。目标结构如下：

```text
qgis_copilot/
├── ui/
│   ├── chat_dock.py                 # 会话信息、快捷任务、卡片、结果动作
│   ├── settings_dialog.py            # 连接、能力档案、模型行为模式
│   ├── view_models.py                # 消息、卡片、可见状态模型
│   └── session_widgets.py            # 仅在 UI 复杂度确实需要时新增
├── application/
│   └── controller.py                 # UI、Agent、QGIS 主线程和后续动作协调
├── agent/
│   ├── core.py
│   ├── events.py                     # 结构化运行轨迹/审计事件
│   └── conversation.py
├── models/
│   ├── base.py
│   ├── capabilities.py               # 模型能力档案与行为模式支持
│   ├── request_options.py            # 按档案构造真实请求参数
│   ├── openai_compatible.py
│   └── settings.py
├── tools/
│   ├── contracts.py
│   ├── registry.py
│   ├── qgis_tools.py                 # 既有基础工具与共享辅助函数
│   ├── diagnostics_tools.py           # 项目/图层质量、CRS、统计
│   ├── query_tools.py                 # 表达式、空间关系、选择集预览
│   └── processing_tools.py            # 写入计划生成与本地参数校验
├── tasks/
│   ├── network.py
│   └── processing.py                 # 统一的确认后 Processing 执行与验证
├── context/
│   └── project_context.py
├── diagnostics/
│   └── logging.py
└── security/
    ├── credentials.py
    └── redaction.py

tests/
├── test_goal6_workbench_ux.py
├── test_goal7_diagnostics_reproject.py
├── test_goal8_spatial_preview_export.py
└── test_goal9_overlay_delivery.py
```

文件名可以因真实职责微调；若现有 `qgis_tools.py` 继续承担所有功能并明显膨胀，必须在同一 Goal 中按上述边界拆分并保持公开工具契约稳定。

---

## Goal 6：可信会话工作台与模型能力档案

### 目标

将现有“聊天面板”升级为用户能快速理解当前项目、模型连接、执行边界与本轮过程的 GIS 工作台；模型行为模式必须基于真实能力档案，不伪造“思考强度”。

### 进入条件

- 现有 Goal 1–5 已通过，当前 QGIS 运行时仍为 QGIS 4.2.1 / Python 3.12.13 / Qt-PyQt 6.11.0；
- 已检查 `ui/chat_dock.py`、`ui/settings_dialog.py`、`application/controller.py`、`models/settings.py`、`models/openai_compatible.py`、`agent/events.py` 与既有测试；
- 不覆盖哥哥已有未提交修改；
- 不修改 `references/QGIS-4.0-MCP-public/`。

### 工作内容

#### Goal 6.1：会话信息条与真实状态

- 在 Dock 顶部增加紧凑的会话信息条，展示当前项目名称、保存状态、图层数量、模型名、接口类型、模型行为模式和执行模式；
- 项目状态从真实 `QgsProject` 与当前模型设置读取，不能硬编码；
- 未保存项目、无模型配置、连接错误或无活动会话应有准确的降级显示；
- 状态统一区分：就绪、模型分析、读取项目、调用工具、等待确认、Processing、完成、失败、取消；
- 状态提示不得显示不存在的处理百分比。

#### Goal 6.2：模型能力档案与模型行为模式

- 增加 `models/capabilities.py`，定义标准 OpenAI-compatible 与后续可扩展档案的能力边界；
- 增加 `models/request_options.py`，由模型档案决定可发送的请求字段；UI 不直接拼装模型私有参数；
- 扩展 `ModelSettings` 与设置窗口，保存非敏感的能力档案和模型行为模式；
- 所有模型至少可选“服务默认”；仅档案明确支持时才启用快速/平衡/深度等模式；
- 不支持时禁用控件并说明“当前模型由服务端控制推理”；
- 不读取或展示模型原始思维链；API Key 安全语义和既有保存/显示规则不得回归。

#### Goal 6.3：空状态、快捷任务与消息卡

- 用真实能力说明替换旧的 Goal 1 占位欢迎文案；
- 增加可编辑快捷任务：项目诊断、图层/CRS、字段统计、表达式筛选、空间关系、处理/导出；
- 快捷任务只能填入输入框或形成待发送文本，不得自动发模型请求或执行操作；
- 将纯文本工具提示升级为统一卡片：自然语言摘要为默认视图，可展开查看工具名、输入、受限返回摘要、耗时、数据范围与错误详情；
- 写入计划卡收敛为统一布局：输入、参数、输出、影响、风险、算法、确认/取消；
- 增加“本轮审计记录”：模型、模式、工具轨迹、输出、未执行写入事项和总耗时。

#### Goal 6.4：真实结果后的 QGIS 操作

- 只在真实可用时提供定位图层、缩放至图层、打开属性表、查看输出路径、复制统计表或继续分析等动作；
- 所有 QGIS UI/画布动作回到主线程；
- 不存在目标图层、结果已移除、未生成输出或当前 QGIS 状态不支持时，隐藏/禁用动作并显示受控错误；
- 不增加自动保存、自动改样式或修改图层的快捷按钮。

### 允许修改文件

- `qgis_copilot/ui/`
- `qgis_copilot/application/controller.py`
- `qgis_copilot/agent/events.py`、`agent/core.py`、`agent/conversation.py`（仅为结构化状态/审计）
- `qgis_copilot/models/`
- `qgis_copilot/context/project_context.py`（仅为安全的项目状态显示）
- `qgis_copilot/diagnostics/logging.py`
- `tests/`
- `README.md`、`架构说明.md`、`task_plan.md`、`findings.md`

### 测试要求

至少覆盖：

- 会话信息条在已保存/未保存项目、已配置/未配置模型下的真实显示数据；
- 标准 OpenAI-compatible 档案只发送通用字段；支持推理的档案才发送已声明的行为字段；
- 不支持模式不可选且有说明；
- API Key 不进入能力档案、普通设置、UI 状态、日志或测试输出；
- 快捷任务可编辑且不会自动发请求；
- 各类状态、工具卡、计划卡、失败/取消卡和审计记录；
- 结果动作只在对应真实对象存在时可用；
- 既有发送、取消、重试、计划确认与插件卸载回归。

### 验证命令

```text
<QGIS安装目录>/bin/python-qgis.bat -m compileall -q qgis_copilot tests
QT_QPA_PLATFORM=offscreen <QGIS安装目录>/bin/python-qgis.bat -m unittest discover -s tests -v
```

### 真实 QGIS Desktop 验收

在一个已保存且至少有矢量/栅格图层的测试项目中：

1. 打开 Dock，核对项目、图层数、模型、接口、模式、执行边界；
2. 使用一个快捷任务，确认只填入可编辑请求而未执行；
3. 发送只读请求，核对模型分析、工具执行、完成与审计记录；
4. 打开工具详情，核对工具名、摘要、耗时和数据范围；
5. 对已有结果图层执行“缩放至图层”或“打开属性表”；
6. 在未配置模型、取消请求和失败情况下核对状态不伪造成功。

### 通过标准

- 用户无需进入设置即可在十秒内理解当前项目、模型连接、能力边界和执行模式；
- 不再出现与当前能力矛盾的 Goal 占位文案；
- 模型行为模式对请求有真实、测试覆盖的影响，或明确显示服务默认；
- 工具、等待确认、Processing、失败、取消和完成状态不混淆；
- 审计记录与实际执行轨迹一致；
- 结果动作在真实 QGIS 中可用且不会产生额外写入；
- 自动化和真实 Desktop 证据均已记录。

### 停止条件

- 需要把模型私有未验证字段作为通用行为模式承诺；
- 需要展示原始思维链、伪造百分比或真实模型不提供的状态；
- 需要引入 Web 前端或独立服务；
- 需要为快捷任务增加未经确认的写入；
- 无法完成真实 Desktop 验收。

---

## Goal 7：数据诊断与安全重投影闭环

### 目标

让 Copilot 能判断当前项目是否适合空间分析，解释数据/CRS 风险，并通过确认式重投影生成可验证的新结果图层。

### 进入条件

- Goal 6 已通过；
- 已确认初始模型上下文仍必须保持轻量，不上传完整属性表、数据源路径、范围、选择集或无界要素；
- 已检查既有 Tool Registry、只读工具契约、ExecutionPlan、Processing 任务和当前缓冲区回归；
- 使用独立临时 QGIS 项目和虚构/公开小数据作为测试证据。

### 工作内容

#### Goal 7.1：新增受限只读诊断工具

实现并注册：

- `get_project_diagnostics`：失效图层、空图层、活动编辑状态、未保存项目状态与 CRS 风险；
- `check_crs_consistency`：项目或指定图层 CRS 的一致性、风险和建议；
- `validate_layer`：矢量字段、空几何、无效几何等质量检查；栅格返回适用元数据并明确不适用的矢量检查；
- `get_layer_statistics`：字段空值、唯一值及受限数值统计；
- `validate_expression` / `select_by_expression_preview`：表达式校验、预计命中数量、有限样本；不得改变选择集；
- 当前选择集摘要：只读返回有限的数量与属性摘要。

每个工具必须定义稳定名称、模型说明、JSON Schema、`READ_ONLY` 权限、输入/返回量上限、图层类型限制、结构化错误与敏感数据边界。

#### Goal 7.2：诊断到处理的建议连接

- 诊断结果可以生成不执行的推荐动作，例如“为道路图层创建 EPSG:3857 重投影计划”；
- 建议不得绕过模型工具契约、本地参数校验或用户确认；
- 对模糊目标 CRS，必须向用户说明风险并请求明确选择，不根据猜测写入；
- 诊断失败或数据不足时明确展示不足，不编造可行建议。

#### Goal 7.3：`reproject_layer` 计划、执行与验证

- 创建新的 `WRITE` 工具：验证输入矢量图层、目标 CRS、输出 `.gpkg` 路径、输出名称、项目状态与路径冲突；
- 计划阶段不得创建目录/文件、修改源图层或添加结果；
- 计划卡展示输入/目标 CRS、转换风险、输出、影响和不覆盖承诺；
- 明确确认后通过 `QgsProcessingAlgRunnerTask` / QGIS Processing 执行；
- 结果必须验证文件存在、能被 QGIS 重开、目标 CRS 正确、图层有效，才加入项目；
- 失败、取消和路径冲突不显示成功，临时数据按现有安全规则清理；
- 将 Processing 执行器泛化到真实的共享职责，不能复制粘贴一个专属重投影任务类后失控。

### 允许修改文件

- `qgis_copilot/tools/contracts.py`、`registry.py`、`qgis_tools.py`、`diagnostics_tools.py`、`query_tools.py`、`processing_tools.py`
- `qgis_copilot/context/project_context.py`
- `qgis_copilot/tasks/processing.py`
- `qgis_copilot/application/controller.py`
- `qgis_copilot/ui/`（仅诊断/重投影卡片与后续动作）
- `qgis_copilot/agent/`、`diagnostics/`
- `tests/`、项目文档

### 测试要求

至少覆盖：

- 矢量、栅格、空图层、失效层、未保存项目、活动编辑状态的受控诊断结果；
- CRS 一致/不一致和指定图层边界；
- 空几何、无效几何与字段统计边界；
- 表达式有效/无效、预览命中数、样本和“选择集不变”；
- 所有诊断工具的 READ_ONLY 权限、参数 schema、未知/无效输入结构化失败；
- 重投影计划阶段零文件、零项目写入；
- 目标 CRS 无效、输出已存在、未保存项目、路径不为 `.gpkg`、用户取消、Processing 失败；
- 确认后输出真实可重开、目标 CRS 正确、源图层要素数/源路径保持不变；
- 既有缓冲区处理、模型工具协议、主线程边界和安全日志回归。

### 验证命令

```text
<QGIS安装目录>/bin/python-qgis.bat -m compileall -q qgis_copilot tests
QT_QPA_PLATFORM=offscreen <QGIS安装目录>/bin/python-qgis.bat -m unittest discover -s tests -v
```

若组合进程再次出现 QGIS 原生退出崩溃，必须按独立 Goal 测试进程隔离并报告；只有 exit 0 的独立回归可作为绿色证据。

### 真实 QGIS Desktop 验收

1. 用“检查当前项目是否适合空间分析”运行项目诊断；
2. 展开检查卡，核对图层数、CRS 风险、编辑/空图层结论与 QGIS 一致；
3. 运行字段统计和表达式预览，核对其未改变选择集；
4. 从真实 CRS 风险或明确请求生成重投影计划；
5. 分别验收取消计划、输出冲突拒绝、确认成功和 Processing 失败；
6. 成功后核对新图层 CRS、要素数、输出路径，并确认源图层不变。

### 通过标准

- 诊断工具返回受限、真实且可理解的项目/图层状态；
- 没有因诊断扩大模型初始上下文或泄露无界数据；
- 用户能从风险说明理解为什么要处理而非仅看到错误；
- 重投影完整通过计划、确认、执行、验证、加入项目；
- 源数据未覆盖，失败/取消无伪造成功；
- 自动化和真实 Desktop 证据均已记录。

### 停止条件

- 需要自动替用户猜测目标 CRS；
- 需要原地改源图层或默认覆盖文件；
- 需要让预览改变选择集、项目或数据；
- 需要新增未确认的编辑/删除能力；
- 真实 Desktop 验收不可完成。

---

## Goal 8：空间关系预览、裁剪与筛选导出闭环

### 目标

让用户从数据判断进入基础数据整理：先安全预览空间/属性条件，再确认生成裁剪或筛选后的新 GeoPackage。

### 进入条件

- Goal 7 已通过；
- 真实 Desktop 中已验证诊断与重投影使用统一卡片/确认语义；
- 已准备有明确空间关系的最小测试图层（例如点、面、边界）以及已保存的测试项目；
- 不增加原地编辑或自动选择集修改。

### 工作内容

#### Goal 8.1：空间关系与导出前预览

- 实现 `spatial_query_preview`：明确支持的相交、包含、邻近等关系；返回受限数量、有限样本和使用的图层/关系；
- 为筛选导出复用已验证表达式或选择集摘要，不让模型凭未验证表达式直接写入；
- 明确区分三种语义：属性筛选导出、空间条件筛选导出、几何裁剪；
- 预览不得修改选择集、源图层、源文件或项目；
- 图层 CRS、几何类型、输入为空、关系不支持、样本超限等情况返回结构化错误或风险。

#### Goal 8.2：`clip_vector` 计划、执行与验证

- 创建 `WRITE` 工具，校验输入矢量、掩膜矢量、几何/CRS 兼容性、输出路径、名称与冲突；
- 计划卡明确展示输入图层、掩膜图层、是否需要坐标处理、输出、影响、风险与 `native:clip`；
- 确认后 Processing 执行，输出文件重开、结果图层有效后才加回项目；
- 失败/取消/冲突不写成功状态，源图层和掩膜图层不被修改。

#### Goal 8.3：`export_filtered_features` 计划、执行与验证

- 创建 `WRITE` 工具，仅接受已验证的表达式、当前选择集或已验证空间条件作为过滤基础；
- 计划阶段显示过滤来源、预估命中数量、输出路径与“仅导出新文件”的影响；
- 确认后导出新 GeoPackage，验证可重开、要素数与预览/实际条件一致，并添加项目；
- 无匹配要素、选择集变化、表达式失效、输出冲突和取消必须有明确、安全的状态。

### 允许修改文件

- `qgis_copilot/tools/` 中查询、处理、契约和注册模块
- `qgis_copilot/tasks/processing.py`
- `qgis_copilot/application/controller.py`
- `qgis_copilot/ui/`、`agent/`、`context/`、`diagnostics/`
- `tests/` 与受影响项目文档

### 测试要求

至少覆盖：

- 相交、包含、邻近预览的正确关系、无匹配、类型不支持、CRS 风险、样本/数量上限；
- 预览前后项目图层、选择集、源数据不变；
- 裁剪计划零副作用、取消、路径冲突、无交集、Processing 失败、成功可重开；
- 筛选导出对有效表达式、选择集、空间条件的受限处理；无匹配/选择集变化/表达式失效的拒绝语义；
- 输出图层加入项目、源图层不变、输出不覆盖；
- 既有 buffer/reproject 与 UI 计划卡/审计轨迹回归。

### 验证命令

```text
<QGIS安装目录>/bin/python-qgis.bat -m compileall -q qgis_copilot tests
QT_QPA_PLATFORM=offscreen <QGIS安装目录>/bin/python-qgis.bat -m unittest discover -s tests -v
```

### 真实 QGIS Desktop 验收

1. 执行空间关系预览，核对不修改选择集；
2. 请求裁剪，核对计划中的输入、掩膜、输出、风险，分别取消和确认；
3. 请求按有效条件导出，核对预览数量、计划、输出新图层和源数据不变；
4. 分别验证已有输出、无匹配和 Processing 失败；
5. 成功后使用结果卡执行缩放/属性表等后续动作。

### 通过标准

- 用户能理解预览、筛选导出和几何裁剪的区别；
- 预览不改变任何 QGIS 工作状态；
- 裁剪与导出均走统一安全写入闭环；
- 输出可验证、可加入项目、可继续使用；
- 自动化与 Desktop 证据均已记录。

### 停止条件

- 需要把“预览”实现成修改选择集或源数据；
- 需要不经确认批量导出、覆盖或修改；
- 需要引入栅格/DEM 或任意 Processing 算法来绕开当前边界；
- 真实 Desktop 验收不可完成。

---

## Goal 9：高频矢量叠加分析、统一交付与回归

### 目标

补齐相交和融合两类高频矢量处理，在统一工作台体验下完成完整质量回归、用户文档更新和干净 QGIS 安装路径验证。

### 进入条件

- Goal 8 已通过；
- 已确认 `ExecutionPlan`、Processing 执行器、计划卡、审计记录和输出验证可以复用；
- 当前范围仍只包含新输出图层，不包含原地字段编辑、删除、保存项目、样式自动化、栅格/DEM 或任意代码。

### 工作内容

#### Goal 9.1：`intersection` 安全闭环

- 验证两个矢量输入、几何/CRS 语义、字段保留策略、输出路径与冲突；
- 计划展示输入、保留字段规则、输出、影响、风险、算法；
- 确认后使用 QGIS Processing 执行，验证可重开和结果有效后加入项目；
- 无交集、字段冲突、取消、失败均为明确受控结果。

#### Goal 9.2：`dissolve` 安全闭环

- 验证输入图层、分类字段或明确的全量融合规则、输出路径与冲突；
- 计划显示融合字段、输出、潜在几何变化和风险；
- 确认后执行，验证输出与源数据不变；
- 不允许模型默默选择不明确的分类字段。

#### Goal 9.3：用户交付、回归和支持边界

- 更新 README：当前工作台能力、模型行为模式语义、数据发送边界、诊断/处理任务、确认流程、结果动作、已知限制；
- 更新架构说明：模型能力档案、结构化审计事件、诊断工具边界与统一安全 Processing 架构；
- 更新 `task_plan.md`、`findings.md`：区分自动化、真实 Desktop、干净安装与未覆盖范围；
- 更新打包白名单/脚本并验证 ZIP 只有一个顶层插件目录，排除测试、参考仓库、缓存、真实数据和密钥；
- 执行敏感信息扫描，覆盖未跟踪新增文件、ZIP 内容和 Git 跟踪内容；
- 用干净 QGIS 路径完成安装、配置、诊断、预览、重投影/裁剪/导出/相交/融合中至少一条完整代表性用户路径；其余写入工具需在真实 Desktop 各自完成成功/取消/失败证据，不能以单个路径替代。

### 允许修改文件

- `qgis_copilot/tools/`、`tasks/processing.py`、`application/controller.py`、`ui/`、`agent/`、`diagnostics/`
- `tests/`、`scripts/package_plugin.py`、`.gitignore`
- `README.md`、`架构说明.md`、`产品需求文档.md`、`task_plan.md`、`findings.md`

### 测试要求

至少覆盖：

- 相交/融合的计划阶段无副作用；
- 输入/字段/CRS/输出参数校验、冲突、无交集、取消、失败与成功；
- 每个成功输出能重开、有效、加入项目，源图层不变；
- 全部新增工具的权限、JSON Schema、模型协议、主线程边界和日志脱敏；
- Goal 6–8 的会话信息、模型能力、审计记录、诊断、预览和 Processing 回归；
- ZIP 结构、敏感文件排除和 README 声明与实际能力一致。

### 验证命令

```text
<QGIS安装目录>/bin/python-qgis.bat -m compileall -q qgis_copilot tests scripts
QT_QPA_PLATFORM=offscreen <QGIS安装目录>/bin/python-qgis.bat -m unittest discover -s tests -v
<QGIS安装目录>/bin/python-qgis.bat scripts/package_plugin.py
```

若 QGIS 原生退出崩溃，隔离测试组并如实记录；不将退出码非零的组合测试称为完整绿色。

### 真实 QGIS Desktop 验收

在干净或隔离的 QGIS 配置中：

```text
安装 ZIP
→ 启用插件
→ 打开 QGIS Copilot
→ 配置已授权模型（由哥哥亲自输入密钥）
→ 查看会话信息条
→ 运行项目诊断
→ 运行空间关系预览
→ 生成并取消一个写入计划
→ 确认执行一个写入计划
→ 验证输出、结果动作和源数据不变
→ 重启 QGIS 并确认配置复用
```

另在真实 Desktop 分别记录 `reproject_layer`、`clip_vector`、`export_filtered_features`、`intersection`、`dissolve` 的成功、取消/拒绝与失败/冲突路径。

### 通过标准

- 相交和融合与既有写入工具采用一致的计划、确认、执行、验证、审计体验；
- 所有新增只读/写入能力都具备明确的权限、输入边界、错误语义和真实 QGIS 证据；
- UI、产品需求、架构、README、任务状态和技术发现一致，不保留过时 MVP/Goal 文案；
- 打包产物安全、结构正确、干净 QGIS 可安装；
- 不把自动化、ZIP 生成或单条截图冒充所有 Desktop 用户路径；
- 本轮范围之外的栅格/DEM、制图、报告、自动执行、任意代码、MCP 与原地编辑仍明确不做。

### 停止条件

- 需要扩大至栅格/DEM、自动制图、报告、任意代码、MCP、自动执行或原地编辑；
- 需要对已有文件启用默认覆盖；
- 发现真实用户路径必须依赖新的运行时、云端账号系统或未确认数据外发；
- 无法完成干净 QGIS 安装或各写入工具的真实 Desktop 验收。

## 推荐执行顺序

```text
Goal 6 可信会话工作台与模型能力档案
  ↓
Goal 7 数据诊断与安全重投影闭环
  ↓
Goal 8 空间关系预览、裁剪与筛选导出闭环
  ↓
Goal 9 高频矢量叠加分析、统一交付与回归
```

## 第一轮执行范围

第一轮只执行 **Goal 6：可信会话工作台与模型能力档案**。

第一轮只改用户能直接看到和理解的会话体验：真实会话信息、模型能力档案、模型行为模式、快捷任务、结构化过程卡、审计记录与结果后的 QGIS 操作。不得在同一轮混入新诊断工具、重投影、裁剪、导出、相交或融合实现。

Goal 6 验收通过后，直接进入 Goal 7；除非触发停止条件，不重新讨论总体架构。

### 已确认的 Goal 7 后续体验修复：内存会话历史

- 在当前 QGIS Desktop 运行期间，“新建会话”应保留旧会话以供查看/切换，而不是销毁旧消息；
- 会话历史仅为内存态，关闭 QGIS 后清空；不得新增聊天持久化、项目写入或敏感数据落盘；
- 会话切换必须保留各自消息、工具卡、审计记录、Agent 上下文与待确认计划，并禁止在网络请求或 Processing 运行时切换。

## 当前不做

- 不修改 `references/QGIS-4.0-MCP-public/`；
- 不引入 TypeScript、Node.js、React、WebEngine、MCP、独立后端或新运行时依赖；
- 不强制接入 Hermes、Claude Desktop、Cursor 或其他外部 Agent；
- 不默认开放任意 PyQGIS 代码执行；
- 不提供自动执行模式；
- 不实现全量 QGIS Processing、栅格/DEM、自动制图、布局导出或报告；
- 不原地批量编辑属性、删除要素/字段、自动保存项目或覆盖已有输出；
- 不上传真实 QGIS 项目、图层数据、API Key 或个人路径；
- 不在未完成真实用户路径验收前发布新版本；
- 不把 Git 提交、推送、ZIP 生成或测试片段当作产品完成替代品。

## 本轮完成定义

本轮只有同时满足以下条件，才能称为完成：

- 用户能在 QGIS 中理解当前项目、模型、模型行为模式、执行边界和本轮过程；
- 不支持的模型能力不会被伪造为可用；
- 用户能完成项目诊断、CRS/质量/统计/筛选/空间关系预览，且不修改项目或源数据；
- 用户能对重投影、裁剪、筛选导出、相交、融合和既有缓冲区完成计划、确认、执行、验证、结果操作的安全闭环；
- 网络与 Processing 不冻结 QGIS，取消、失败、冲突和超时不伪造成功；
- 输出默认不覆盖、可重开、可加入项目，源数据不变；
- API Key、完整敏感数据和不应公开的路径不进入普通设置、日志、聊天记录、测试、ZIP 或仓库；
- 自动化、真实 Desktop、干净安装和文档一致性证据均按边界如实记录；
- README、产品需求、架构、任务状态和技术发现与实际能力一致。
