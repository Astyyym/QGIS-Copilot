# QGIS Copilot 开发短计划

## 目标

在不要求用户安装外部 Agent 或手动配置 MCP 的前提下，完成一个能在 QGIS 内聊天、读取项目上下文、执行少量安全 GIS 工具并反馈真实结果的最小垂直切片。

## 阶段 0：产品与环境基线

### 目标

验证首发 QGIS 版本、运行平台、模型接入方式、UI 技术路线和许可证边界，并建立可加载的插件骨架。

### 工作

- 确认首发支持的 QGIS 版本范围；
- 确认插件目录结构和最小可加载方式；
- 使用 Python + qgis.PyQt Qt Widgets 验证聊天 UI；
- 明确模型配置和密钥保存方案；
- 明确 MVP 工具清单与权限分类。

### 技术基线

- 首发全 Python；
- QGIS 插件、Agent Core、模型适配和 PyQGIS 工具同进程；
- 不引入 TypeScript、Node.js、React 或独立后端；
- 原生 QGIS 工具不强制经过 MCP；
- MCP 作为后续扩展边界。

### 验证

- 在目标 QGIS 环境加载最小插件；
- 打开并关闭聊天面板；
- 验证模型请求不会阻塞 QGIS 主界面；
- 验证配置不会写入敏感信息到项目源码。

## 阶段 1：聊天壳与模型适配

### 目标

实现一个可输入消息、显示响应、处理配置错误的聊天面板。

### 验证

- 能完成一次真实模型请求；
- 能显示流式或分段响应；
- 网络失败、认证失败和超时有真实可读提示；
- QGIS 地图画布和主界面保持可操作。

## 阶段 2：只读 QGIS 上下文

### 目标

让 Agent 能理解当前项目和图层基本状态。

### 首批工具

- 获取项目状态；
- 列出图层；
- 查看图层字段和 CRS；
- 查询有限数量的属性；
- 获取基础统计信息。

### 验证

用真实 QGIS 项目完成至少三类只读请求，并核对回答与 QGIS 实际状态一致。

## 阶段 3：第一个安全写入闭环

### 目标

实现一条完整的“自然语言 → 计划 → 用户确认 → QGIS 执行 → 结果反馈”链路。

### 建议场景

使用一个明确输入图层执行缓冲区或裁剪，并将结果输出到新文件，不覆盖原始数据。

### 验证

- 计划显示输入、参数、输出和风险；
- 取消不会产生写入；
- 确认后结果真实生成；
- 结果可被 QGIS 重新加载；
- 失败时不显示成功。

## 阶段 4：权限、安全和诊断

### 目标

建立可用于公开测试的安全边界和故障反馈。

### 工作

- 只读、确认、自动执行模式；
- 工具风险分类；
- 取消和超时；
- 操作日志；
- API Key 安全存储；
- 本地连接和外部 MCP 授权边界；
- 敏感信息扫描。

## 阶段 5：发布前验收

### 目标

验证普通用户安装、配置、聊天和完成任务的完整路径。

### 验证

- 干净 QGIS 环境安装；
- 无外部 Agent、uv 或手动 MCP 配置；
- Windows、Linux、macOS 中按支持矩阵验证；
- 真实用户路径验收；
- 插件包内容、许可证和 README 检查；
- 开源敏感信息扫描。

## 当前不做

- 不先实现全部 GIS 工具；
- 不先制作复杂网页前端；
- 不强制接入 Hermes；
- 不默认开放任意 PyQGIS 代码执行；
- 不在需求未确认前发布或推送仓库。
- 不因为 Agent 或 MCP 的生态需要而提前拆分 Node/TypeScript 服务；
- 不把 WebEngine 作为聊天界面的首发依赖。
