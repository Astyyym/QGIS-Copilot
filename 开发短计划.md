# QGIS Copilot 开发短计划

> 版本：v1.1
> 状态：可供 Goal 模式直接执行
> 产品：QGIS Copilot
> 目标：先完成一个可加载、可聊天、可读取当前 QGIS 项目、可执行一次安全 GIS 操作的最小垂直切片
> 权威项目目录：`D:\wenjian\Hermes\QGIS Copilot`

## 执行原则

- 先搭可运行的 QGIS 插件骨架，再逐步打通聊天、Agent、QGIS 工具和安全执行。
- 每个 Goal 都应能独立验收，完成后再进入下一个 Goal。
- 按功能和职责拆分文件，不把插件生命周期、UI、模型请求、Agent 和 PyQGIS 逻辑堆在一个文件中。
- 第一版只做单机使用和当前 QGIS 项目操作，不接入外部 Agent 作为前置依赖。
- QGIS 原生工具直接调用 PyQGIS；MCP 只作为后续扩展边界。
- 首发使用 Python + qgis.PyQt Qt Widgets + Python Agent Core，不引入 TypeScript、Node.js、React、WebEngine 或独立后端进程。
- 网络请求不能阻塞 QGIS 主界面，PyQGIS 操作必须遵守 QGIS 主线程约束。
- 每完成一个 Goal 都运行对应的真实验证，并把结果记录到 `task_plan.md` 和 `findings.md`。
- 不用模拟响应、静态假数据或“代码看起来完成”替代真实 QGIS 运行结果。

## Goal 模式执行合同

### 总目标

在 QGIS 内交付一个不依赖 Node.js、TypeScript、React、Hermes、uv 或独立后端进程的 Python AI GIS 插件。用户安装插件后，可以打开聊天面板，用自然语言读取当前项目，并在确认后执行一个不会覆盖原始数据的 GIS 操作。

### 允许修改的范围

- 项目根目录内的正式代码、测试、文档和构建配置；
- `references/QGIS-4.0-MCP-public/` 仅供阅读，禁止修改；
- 测试数据必须使用虚构数据或公开小型数据，不使用哥哥的真实项目数据进入仓库。

### 停止条件

遇到以下情况必须停止当前 Goal，记录真实原因并让哥哥判断：

- 需要改变已确定的总体架构或编程语言；
- 需要引入 Node.js、TypeScript、独立服务或新的运行时依赖；
- 需要上传真实图层、项目或 API Key；
- 需要开放任意 PyQGIS 代码执行；
- 需要删除、覆盖或迁移哥哥已有的项目数据；
- 目标 QGIS 未安装或无法启动，导致真实验收无法完成；
- 需求、权限边界、数据模型或交付范围发生变化。

普通实现细节不确定时，先查项目文件、官方文档和实际环境，不要把可查问题升级成阻塞。

### 完成条件

一个 Goal 只有在其验收标准全部通过、真实证据已记录、工作区没有未说明的调试残留后，才算完成。

## 目标目录结构

正式代码完成后应按职责接近以下结构：

```text
qgis_copilot/
├── __init__.py
├── metadata.txt
├── plugin.py
├── ui/
│   ├── __init__.py
│   ├── chat_dock.py
│   ├── settings_dialog.py
│   └── view_models.py
├── application/
│   ├── __init__.py
│   └── controller.py
├── agent/
│   ├── __init__.py
│   ├── core.py
│   ├── conversation.py
│   └── events.py
├── models/
│   ├── __init__.py
│   ├── base.py
│   ├── openai_compatible.py
│   └── settings.py
├── tools/
│   ├── __init__.py
│   ├── contracts.py
│   ├── registry.py
│   ├── permissions.py
│   └── qgis_tools.py
├── context/
│   ├── __init__.py
│   └── project_context.py
├── tasks/
│   ├── __init__.py
│   ├── network.py
│   └── processing.py
├── security/
│   ├── __init__.py
│   ├── credentials.py
│   └── redaction.py
└── diagnostics/
    ├── __init__.py
    └── logging.py

tests/
├── unit/
├── integration/
└── fixtures/

docs/
└── plans/
```

目录可以按实际需要增加文件，但不得破坏职责边界。没有真实职责前，不要创建空的未来扩展模块。

## Goal 1：建立可加载的 QGIS 插件与聊天工作台

### 目标

让 QGIS 能识别、启用并卸载 QGIS Copilot，点击菜单或工具栏可以打开一个可用的原生 Qt 聊天面板。

### 工作内容

- 先检查目标 QGIS 版本、内置 Python、插件目录和当前 Git 状态；
- 创建 `qgis_copilot/__init__.py` 和 `classFactory(iface)`；
- 创建 `qgis_copilot/metadata.txt`、`plugin.py` 和 `LICENSE`；
- 注册菜单项、工具栏动作和插件卸载逻辑；
- 创建 `ui/chat_dock.py`、`ui/view_models.py` 和 `application/controller.py`；
- 实现消息列表、输入框、发送按钮、停止按钮、设置入口和空状态；
- 实现发送中、完成、失败、取消等基础 UI 状态；
- 使用 `qgis.PyQt` Qt Widgets，不引入 WebEngine；
- 建立本地开发插件目录映射，不复制第二份权威源码；
- 创建结构检查或最小单元测试。

### 文件要求

```text
qgis_copilot/
├── __init__.py
├── metadata.txt
├── plugin.py
├── ui/
│   ├── __init__.py
│   ├── chat_dock.py
│   └── view_models.py
├── application/
│   ├── __init__.py
│   └── controller.py
└── LICENSE
```

### 验收

- 插件在目标 QGIS 中可被发现并启用；
- 没有 ImportError、启动 traceback 或资源加载错误；
- 菜单或工具栏入口可见；
- 点击入口能打开 Dock Widget；
- Dock Widget 可以关闭并重新打开；
- 能输入消息，空白消息不会发送；
- 发送中可以取消，失败后可以重试；
- 禁用或卸载插件后动作、窗口和信号被正确清理；
- QGIS 主窗口和地图画布仍正常工作；
- QGIS 中的实际点击验证已记录。

## Goal 2：接入模型并建立 Agent 基础循环

### 目标

让用户可以配置模型并完成一次真实聊天请求，同时保证网络请求不冻结 QGIS。

### 工作内容

- 在 `models/base.py` 定义统一模型适配接口；
- 在 `models/openai_compatible.py` 实现 OpenAI-compatible HTTP 请求；
- 在 `models/settings.py` 实现 API 地址、模型名、超时和配置校验；
- 在 `security/credentials.py` 使用 QGIS 认证存储保存 API Key；
- 在 `security/redaction.py` 过滤密钥和 Authorization Header；
- 在 `tasks/network.py` 实现工作线程或等价的非阻塞网络任务；
- 在 `agent/conversation.py` 管理会话消息；
- 在 `agent/events.py` 定义结构化 Agent 事件；
- 在 `agent/core.py` 实现最小 Agent 循环、最大步数、超时和取消；
- 在 `ui/settings_dialog.py` 实现设置界面和连通性测试；
- 处理认证失败、超时、断网、无效响应和用户取消。

### 依赖规则

- 优先使用 Python 标准库和 QGIS 自带模块；
- 不要求用户手动执行 pip、uv 或安装 Node.js；
- 引入第三方依赖前必须证明目标 QGIS 环境能够稳定提供，不能破坏 QGIS 自带依赖；
- 不把真实 API Key 写入测试文件、日志或 Git。

### 验收

- 未配置模型时能打开设置向导，不进入假聊天；
- 能保存并读取模型地址、模型名和认证配置；
- API Key 不出现在普通配置文件、日志和错误消息中；
- 使用测试模型端点完成一次真实请求；
- 请求期间 QGIS 地图画布仍可操作；
- 认证失败、超时和断网时显示真实错误；
- 取消请求后不会把取消显示为成功；
- Agent 能限制最大调用步数，不会无限循环；
- 模型适配层不依赖 UI 或 PyQGIS 对象。

## Goal 3：接入只读 QGIS 上下文和工具

### 目标

打通第一条真实 Agent 链路：自然语言请求 → 读取当前 QGIS 项目 → 调用只读工具 → 返回真实回答。

### 工作内容

- 在 `context/project_context.py` 生成最小必要项目摘要；
- 在 `tools/contracts.py` 定义工具名称、参数、权限等级和结果结构；
- 在 `tools/registry.py` 实现工具注册、发现和调用入口；
- 在 `tools/permissions.py` 实现只读工具判定；
- 在 `tools/qgis_tools.py` 实现以下工具：
  - `get_project_state`；
  - `list_layers`；
  - `inspect_layer`；
  - `query_features`；
- 所有 PyQGIS 访问回到 QGIS 主线程；
- 只提供必要的图层、字段、CRS、范围、选择集和有限属性样本；
- 对不存在的图层、无效参数和空项目返回结构化结果；
- 在 Agent 事件中展示工具调用进度和结果摘要。

### 验收

用真实 QGIS 项目完成并核对以下请求：

1. “当前项目有哪些图层？”；
2. “告诉我某个图层的字段和 CRS。”；
3. “查询某个图层前 N 条属性。”。

必须满足：

- 工具结果与 QGIS 实际状态一致；
- 只读请求不修改项目、图层和源数据；
- 工具调用过程在聊天面板可见；
- 模型选错工具或参数无效时不会崩溃；
- 超过最大步数后停止并提示真实原因；
- 大规模要素不会未经限制地发送给模型；
- 单元测试覆盖工具契约，QGIS 实例验证真实行为。

## Goal 4：完成安全的 GIS 执行闭环

### 目标

完成一个安全的自然语言 GIS 操作：生成缓冲区或裁剪结果到新文件，经过用户确认后加入当前项目。

### 工作内容

- 在 `tools/permissions.py` 区分只读、可写、破坏性和代码执行工具；
- 在 `tools/contracts.py` 增加执行计划、影响范围和确认状态；
- 在 `ui/view_models.py` 增加计划卡片、确认和取消交互；
- 在 `tasks/processing.py` 使用 QGIS Processing 任务机制；
- 在 `tools/qgis_tools.py` 实现一个输入明确、输出为新文件的空间处理工具；
- 优先实现道路图层缓冲区或矢量裁剪，不覆盖原始数据；
- 执行前校验输入图层、参数、CRS、输出路径和文件冲突；
- 输出文件存在时默认拒绝覆盖；
- 执行后验证输出文件和结果图层；
- 成功后将结果图层加入当前项目；
- 失败、取消和用户拒绝都不能显示为成功。

### 推荐场景

```text
用户：给道路图层创建 500 米缓冲区，输出为新的 GeoPackage 图层。
```

### 验收

- Agent 不直接执行写入，而是先展示计划；
- 计划显示输入图层、参数、输出路径、预计影响和风险；
- 用户点击取消后不生成文件、不添加图层；
- 用户确认后真实执行 Processing；
- 结果文件存在且可被 QGIS 重新打开；
- 结果图层被添加到当前项目；
- 原始图层和源文件未被覆盖；
- Processing 报错时显示真实错误和失败状态；
- 任务运行时 QGIS 主界面仍可响应；
- 对确认、取消、成功和失败分别留下可复核证据。

## Goal 5：完成安全、测试、打包和真实用户路径验收

### 目标

使 MVP 可以交给其他用户安装测试，并具备基本的安全边界、诊断能力和可重复交付方式。

### 工作内容

- 在 `security/` 完成凭据安全存储、权限判定和日志脱敏；
- 在 `diagnostics/logging.py` 记录工具名、状态、耗时和摘要；
- 默认提供只读模式和确认执行模式；
- 默认禁用任意 PyQGIS 代码执行；
- 加入 Agent 最大步数、网络超时、Processing 超时和取消；
- 对删除、覆盖、批量编辑和保存项目设置更高等级确认；
- 对项目未保存、图层失效、输出冲突和 CRS 风险给出明确提示；
- 在 `tests/unit/` 测试模型配置、工具契约、权限、脱敏和 Agent 步数；
- 在 `tests/integration/` 测试插件加载、项目上下文和工具执行；
- 在 `tests/fixtures/` 使用虚构或公开的小型测试数据；
- 创建可重复的插件打包脚本；
- 打包时排除 `.git`、缓存、密钥、测试数据库和 `references/`；
- 确保 ZIP 只有一个顶层插件目录，并包含 `metadata.txt`、`__init__.py` 和 `LICENSE`；
- 完善 README 的安装、配置、隐私、权限和已知限制；
- 执行敏感信息扫描、语法检查、测试和差异检查。

### 验收

完整走通：

```text
干净 QGIS
→ 安装插件
→ 打开 Copilot
→ 配置模型
→ 查询当前项目
→ 请求创建新结果
→ 查看执行计划
→ 确认
→ 验证结果图层
→ 关闭并重新打开 QGIS
```

必须记录：

- QGIS 版本；
- 操作系统；
- 安装方式；
- 测试数据；
- 执行请求；
- 实际结果；
- 失败或未覆盖的场景。

只有在真实用户路径通过后，才能把 MVP 称为完成。语法检查、模块导入或插件 ZIP 生成不能替代这条验收。

## 推荐执行顺序

```text
Goal 1 插件与聊天工作台
  ↓
Goal 2 模型与 Agent 基础循环
  ↓
Goal 3 只读 QGIS 上下文和工具
  ↓
Goal 4 安全 GIS 执行闭环
  ↓
Goal 5 安全、测试、打包和真实用户路径验收
```

## 第一轮只执行什么

第一轮只执行 **Goal 1：建立可加载的 QGIS 插件与聊天工作台**。

第一轮先完成环境检查、最小插件骨架和原生 Qt 聊天面板。不要在第一轮同时接入真实模型、Agent 工具、Processing 写入或 MCP。

Goal 1 验收通过后，直接进入 Goal 2，不需要重新讨论总体架构。

## 当前不做

- 不修改 `references/QGIS-4.0-MCP-public/`；
- 不先实现全部 QGIS Processing 算法；
- 不引入 TypeScript、Node.js、React、WebEngine 或独立后端；
- 不强制接入 Hermes、Claude Desktop、Cursor 或其他外部 Agent；
- 不默认开放任意 PyQGIS 代码执行；
- 不上传真实 QGIS 项目、图层数据、API Key 或个人路径；
- 不在未完成真实用户路径验收前发布插件；
- 不把 Git 提交、推送或 Release 当作产品完成的替代品。

## 完成定义

QGIS Copilot 的 MVP 只有同时满足以下条件，才能称为完成：

- 用户只安装插件即可打开聊天界面；
- 不依赖 Hermes、Node.js、uv 或手动 MCP 配置；
- 能读取当前 QGIS 项目并返回真实信息；
- 能完成一次计划、确认、执行、验证、反馈的安全写入操作；
- 网络请求不会冻结 QGIS；
- 失败、取消和超时不会伪造成功；
- API Key 和敏感项目内容不会进入公开仓库或普通日志；
- 有真实 QGIS 用户路径验证证据；
- README、插件包和支持矩阵与实际情况一致。
