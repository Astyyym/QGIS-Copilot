# QGIS Copilot 架构说明

## 1. 总体原则

QGIS Copilot 面向普通 QGIS 用户，优先保证安装简单、行为可解释、风险可控和跨平台可维护。

产品不把 MCP、外部 Agent、端口或用户本机 Python 环境暴露给普通用户。

## 2. 推荐架构

```text
┌──────────────────────────────────────────┐
│                  QGIS                    │
│                                          │
│  ┌────────────────────────────────────┐  │
│  │ QGIS Copilot 插件                  │  │
│  │                                    │  │
│  │  Chat UI                            │  │
│  │    ↓                               │  │
│  │  Agent Core                        │  │
│  │    ├── Context Manager             │  │
│  │    ├── Model Adapter               │  │
│  │    ├── Tool Planner                │  │
│  │    ├── Permission / Confirmation   │  │
│  │    └── Conversation State          │  │
│  │         ↓                          │  │
│  │  Native QGIS Tool Adapter          │  │
│  │         ↓                          │  │
│  │  PyQGIS / QGIS Processing           │  │
│  │                                    │  │
│  │  Optional MCP Client / Server      │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
              │
              ├── 云端 OpenAI-compatible API
              ├── 本地模型服务
              └── 外部 MCP 服务（可选）
```

## 3. 模块边界

### Chat UI

负责聊天消息、输入状态、工具调用进度、执行计划、确认按钮、错误提示和结果展示。不负责直接调用 PyQGIS。

### Agent Core

负责多轮对话、上下文压缩、工具选择、工具调用循环、超时、取消和错误恢复。不应把特定模型厂商逻辑写死在核心中。

### Model Adapter

负责不同模型服务之间的请求格式、认证、流式响应和工具调用格式适配。核心层只依赖统一接口。

### Context Manager

按最小必要范围提供当前项目、图层、字段、选择集和处理环境信息，避免每次把整个项目或所有要素发送给模型。

### Native QGIS Tool Adapter

将安全、可测试的业务工具映射到 PyQGIS 和 Processing。优先提供语义明确的深模块工具，而不是直接暴露大量底层 API。

### Permission / Confirmation

对工具分类：只读、可写、破坏性、任意代码。默认按风险要求确认，并提供用户可见的影响范围。

### Optional MCP Layer

用于接入外部工具或让其他 MCP Host 连接 QGIS Copilot。它不是内置 QGIS 操作的必经网络链路。

## 4. 工具设计原则

- 工具参数明确、可校验、可记录；
- 写入型工具支持 dry-run 或预览；
- 删除和覆盖必须有明确目标，禁止无筛选批量执行；
- 输出路径由工具层校验；
- 工具返回结构化结果，包含成功状态、影响范围、输出对象和真实错误；
- 工具不把内部异常直接变成“成功”；
- 任意 PyQGIS 代码执行不作为普通模式的默认能力。

## 5. 进程与依赖原则

- 不要求用户准备独立 Python 环境；
- 不要求用户通过 pip、uv 或手动配置安装依赖；
- 尽量使用 QGIS 自带的 Python、PyQt 和标准库；
- 需要额外运行时的部分必须有可重复的跨平台打包方案；
- 不在 QGIS 插件中随意覆盖 QGIS 自带依赖；
- 聊天 UI 的技术选型必须以目标 QGIS 发行版的真实兼容性为依据。

## 6. 安全边界

- 网络服务默认仅绑定 localhost；
- 外部 MCP 连接需要明确授权；
- API Key 使用 QGIS 认证存储或平台安全存储；
- 日志不得包含 API Key、完整敏感数据或不必要的图层内容；
- 默认关闭或限制任意代码执行；
- 高风险操作必须可见、可取消，并记录操作结果；
- 文件访问范围和输出目录应有明确边界。

## 7. 需要通过验证后才能锁定的内容

- QGIS 3.x 与 4.x 的兼容策略；
- Qt Widgets 与 Qt WebEngine 的实际可用性；
- 模型服务的流式传输和工具调用兼容性；
- QGIS 主线程限制下的网络请求和工具执行调度；
- 插件发布、依赖打包和自动更新方式。
