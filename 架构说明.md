# QGIS Copilot 最终架构说明

## 1. 架构结论

QGIS Copilot 采用**单体 Python QGIS 插件架构**：插件本身同时承担 QGIS 插件入口、聊天界面、Agent Host、模型适配、权限控制和 QGIS 工具调度。

首发版本不引入 TypeScript、Node.js、React、独立后端进程或强制 MCP Server。这样做不是因为 TypeScript 不好，而是因为 QGIS 的真实扩展边界是 Python/PyQGIS；在首发阶段增加第二套运行时会直接增加安装、打包、进程、跨平台和故障排查成本。

## 2. 最终运行结构

```text
┌──────────────────────────────────────────────┐
│                    QGIS                      │
│                                              │
│  ┌────────────────────────────────────────┐  │
│  │ QGIS Copilot Python Plugin            │  │
│  │                                        │  │
│  │  Qt Widgets Chat UI                    │  │
│  │          ↓                             │  │
│  │  Application Controller                │  │
│  │          ↓                             │  │
│  │  Agent Core                            │  │
│  │   ├── Conversation State               │  │
│  │   ├── Context Manager                  │  │
│  │   ├── Model Adapter                    │  │
│  │   ├── Tool Planner / Loop              │  │
│  │   ├── Permission Gate                  │  │
│  │   └── Cancellation / Recovery          │  │
│  │          ↓                             │  │
│  │  Native QGIS Tool Registry             │  │
│  │          ↓                             │  │
│  │  PyQGIS / QGIS Processing               │  │
│  │                                        │  │
│  │  Optional MCP Extension Boundary       │  │
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
                 │
                 ├── OpenAI-compatible API（首发）
                 └── 本地模型 HTTP 服务（后续）
```

## 3. 编程语言与技术栈（最终确定）

| 模块 | 技术 | 原因 |
|---|---|---|
| QGIS 插件 | Python | QGIS 官方 Python 插件和 PyQGIS 原生生态 |
| GIS 工具 | Python + PyQGIS | 直接访问项目、图层、Processing 和地图画布 |
| 聊天界面 | Python + qgis.PyQt Qt Widgets | 不依赖 Node/WebEngine，嵌入和分发更稳 |
| Agent Core | Python | 与 QGIS 工具同进程，减少 IPC 和运行时依赖 |
| 模型适配 | Python 标准库优先 | 减少插件依赖冲突，统一不同模型接口 |
| 数据校验 | Python 类型标注与显式校验 | 工具参数和结果可预测、可测试 |
| 外部扩展 | 后续实现 MCP Client/Server | 只作为扩展边界，不成为内置功能前置条件 |

## 4. 用户实际使用流程

### 首次使用

1. 用户在 QGIS 插件管理器安装 QGIS Copilot；
2. 点击工具栏中的“QGIS Copilot”；
3. 插件打开右侧可停靠聊天面板；
4. 首次使用进入设置向导；
5. 用户填写 OpenAI-compatible API 地址、模型名和 API Key；
6. 插件用一次只读连通性请求验证配置；
7. 配置成功后进入聊天界面。

用户不需要安装 Hermes、Node.js、uv、Python，也不需要编辑 MCP 配置文件。

### 一次只读任务

```text
用户：当前项目有哪些图层？每个图层的 CRS 是什么？
    ↓
Agent 读取项目摘要
    ↓
选择 get_project_state / list_layers / inspect_layer
    ↓
工具在 QGIS 主线程执行
    ↓
聊天窗口返回真实结果
```

### 一次写入任务

```text
用户：给道路图层创建 500 米缓冲区，输出到项目目录
    ↓
Agent 读取图层与项目目录
    ↓
生成执行计划和参数
    ↓
展示输入图层、距离、输出文件和风险
    ↓
用户点击“执行”
    ↓
调用 QGIS Processing
    ↓
验证输出文件和结果图层
    ↓
添加结果到当前项目并反馈
```

## 5. 模块职责

### Chat UI

只负责展示和交互：消息、工具进度、计划、确认、取消、错误、结果和设置。不直接操作 PyQGIS。

### Application Controller

连接 UI、Agent、权限层和 QGIS 生命周期；保证插件关闭时请求、任务和资源正确结束。

### Agent Core

负责对话上下文、模型请求、工具选择、多轮调用、最大步数、超时、取消和错误恢复。不得依赖具体 UI，也不得直接调用 PyQGIS。

### Model Adapter

统一处理请求、流式响应、工具调用、认证、超时和错误。首发只实现 OpenAI-compatible HTTP 接口，适配器接口为未来本地模型服务保留边界。

### Context Manager

只提供完成当前任务所需的项目、图层、字段、CRS、选择集和处理环境摘要。大规模要素不能未经筛选整体发送给模型。

### Native QGIS Tool Registry

注册经过权限分类和参数校验的高层工具。工具内部才可以访问 PyQGIS。Agent 只看到稳定的工具契约，不看到 QGIS 内部对象。

### Permission Gate

在工具执行前判断工具风险、目标、影响范围和用户模式。只读工具直接执行；写入、删除、覆盖和保存操作必须展示计划并确认；任意代码执行默认不存在于普通模式。

## 6. 线程与任务模型

- 网络请求在工作线程或异步任务中执行，不能阻塞 QGIS 主界面；
- 所有 PyQGIS 对象访问和 QGIS 状态修改回到 QGIS 主线程；
- Processing 长任务使用 QGIS 可观察的任务机制；
- 每次请求有取消入口、最大执行步数和超时；
- 插件关闭时取消未完成的网络请求和 Agent 循环；
- UI 只接收结构化事件，不直接解析模型原始文本来决定是否执行工具。

## 7. 错误与恢复

| 问题 | 用户看到的处理 |
|---|---|
| 未配置模型 | 打开设置向导，不进入假聊天 |
| API Key 无效 | 显示认证失败，保留输入内容，不声称成功 |
| 网络超时 | 提供重试和取消，标记本次未完成 |
| 模型返回无效工具参数 | 工具校验拒绝执行，让 Agent 修正一次，仍失败则停止 |
| 图层不存在或已变化 | 重新读取项目状态，要求用户确认目标 |
| 输出文件已存在 | 默认不覆盖，要求另选路径或明确确认 |
| Processing 失败 | 显示真实错误，不添加伪造结果图层 |
| QGIS 项目未保存 | 提示保存或选择输出目录，不擅自覆盖项目 |
| 用户关闭 QGIS | 取消请求并清理资源 |

## 8. 数据与安全边界

- API Key 使用 QGIS 认证存储，不写入项目文件、源码和普通日志；
- 默认不上传完整图层数据，只发送最小必要元数据和用户明确要求的样本；
- 文件路径、图层属性和项目内容在发送前应允许用户控制范围；
- 写入工具必须支持预览或影响范围估计；
- 删除、覆盖、批量编辑和保存必须二次确认；
- 日志记录工具名、时间、状态和摘要，不记录密钥和完整敏感数据；
- 外部 MCP 连接后置，并且需要单独授权、隔离和取消机制。

## 9. MCP 的最终定位

MCP 不是 QGIS Copilot 内置 QGIS 操作的必经链路。首发版本先通过内部 Tool Registry 直接调用 PyQGIS，避免为了协议而增加本地网络和进程故障点。

后续 MCP 扩展只承担两类职责：

1. 让 QGIS Copilot 连接外部数据和工具服务；
2. 让其他 MCP Host 在用户授权后连接 QGIS Copilot。

## 10. 目录边界

正式代码按以下职责拆分，文件名可在实现阶段微调，但边界不应混淆：

```text
qgis_copilot/
├── __init__.py
├── metadata.txt
├── plugin.py              # QGIS 插件生命周期
├── ui/                    # Qt 界面和展示模型
├── application/           # 控制器与应用事件
├── agent/                 # Agent Core、上下文和会话
├── models/                # 模型适配与配置
├── tools/                 # 工具契约、注册表和 PyQGIS 实现
├── security/              # 权限、认证存储和敏感信息过滤
├── tasks/                 # 网络与 Processing 任务调度
└── diagnostics/           # 日志、错误和诊断
```

## 11. 架构验收标准

- 在干净 QGIS 环境中只安装插件即可打开界面；
- 无 Node.js、uv、Hermes 或独立后端进程依赖；
- 一次只读任务能返回与 QGIS 实际状态一致的结果；
- 一次写入任务能完整经历计划、确认、执行、验证和反馈；
- 模型网络请求不会冻结 QGIS；
- 取消、超时、模型错误和 Processing 错误均不会伪造成功；
- API Key 和项目敏感内容不进入公开仓库或普通日志。
