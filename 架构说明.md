# QGIS Copilot 最终架构说明

## 1. 架构结论

QGIS Copilot 采用**单体 Python QGIS 插件架构**：插件本身同时承担 QGIS 插件入口、原生聊天工作台、Agent Host、模型适配、权限控制和 QGIS 工具调度。

首发及当前增量阶段不引入 TypeScript、Node.js、React、独立后端进程、WebEngine 或强制 MCP Server。QGIS 的真实扩展边界是 Python/PyQGIS；增加第二套运行时会增加安装、打包、进程、跨平台和故障排查成本，而不能直接提升普通用户体验。

当前架构的产品方向是“可信 GIS 工作台”：先让 Agent 以受限上下文理解项目、以只读工具诊断数据，再通过计划、确认、Processing、验证完成高频空间处理。

## 2. 最终运行结构

```text
┌──────────────────────────────────────────────────────┐
│                         QGIS                         │
│                                                      │
│  ┌────────────────────────────────────────────────┐  │
│  │ QGIS Copilot Python Plugin                      │  │
│  │                                                │  │
│  │  Qt 工作台 UI                                  │  │
│  │  ├─ 会话信息/快捷任务/结果卡                   │  │
│  │  ├─ 工具轨迹/审计记录/计划确认                 │  │
│  │  └─ 设置/模型能力档案                          │  │
│  │                  ↓                             │  │
│  │  Application Controller                         │  │
│  │                  ↓                             │  │
│  │  Agent Core                                     │  │
│  │  ├─ Conversation / 状态事件                     │  │
│  │  ├─ Context Manager                             │  │
│  │  ├─ Model Adapter / 能力档案                    │  │
│  │  ├─ Tool Planner / 有界循环                     │  │
│  │  └─ Permission Gate / 恢复与取消                │  │
│  │                  ↓                             │  │
│  │  Native QGIS Tool Registry                      │  │
│  │  ├─ 只读：诊断、统计、表达式、空间预览         │  │
│  │  └─ 写入：计划→确认→Processing→结果验证        │  │
│  │                  ↓                             │  │
│  │  PyQGIS / QGIS Processing / 地图画布            │  │
│  │                                                │  │
│  │  Optional MCP Extension Boundary                │  │
│  └────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────┘
                 │
                 ├── OpenAI-compatible API（当前）
                 └── 本地模型 HTTP 服务（后续扩展）
```

## 3. 编程语言与技术栈

| 模块 | 技术 | 原因 |
|---|---|---|
| QGIS 插件 | Python | QGIS 官方 Python 插件与 PyQGIS 原生生态 |
| GIS 工具 | Python + PyQGIS | 直接访问项目、图层、Processing、地图画布和属性表 |
| 聊天工作台 | Python + qgis.PyQt Qt Widgets | 原生嵌入、无 Node/WebEngine 依赖、分发稳定 |
| Agent Core | Python | 与 QGIS 工具同进程，减少 IPC 和运行时故障点 |
| 模型适配 | Python 标准库优先 | 降低插件依赖冲突，隔离不同模型协议 |
| 模型能力档案 | Python 数据模型 + 请求选项模块 | 不把厂商参数或伪造能力散落到 UI |
| 数据校验 | Python 类型标注与显式校验 | 工具参数、计划与结果可预测、可测试 |
| 外部扩展 | 后续 MCP Client/Server | 只作为互操作边界，不成为内置能力前置条件 |

## 4. 用户实际使用流程

### 4.1 首次使用与可信会话

1. 用户在 QGIS 插件管理器安装 QGIS Copilot；
2. 点击工具栏或菜单中的“QGIS Copilot”；
3. 插件打开右侧可停靠工作台；
4. 首次使用进入设置向导，填写 OpenAI-compatible API 地址、模型名和 API Key；
5. 插件用一次只读连通性请求验证配置；
6. 工作台显示当前项目、图层数、模型、接口类型、模型行为模式和执行模式；
7. 用户从快捷任务或自然语言请求开始工作。

用户不需要安装 Hermes、Node.js、uv、Python，也不需要编辑 MCP 配置文件。

### 4.2 数据检查与分析

```text
用户：检查这个项目是否适合做空间分析
    ↓
读取轻量项目摘要
    ↓
调用项目诊断 / CRS 检查 / 图层质量 / 字段统计等只读工具
    ↓
工作台显示结论、问题明细、工具轨迹和数据范围
    ↓
用户定位图层、查看详情或选择推荐的处理方案
```

### 4.3 受控空间处理

```text
用户：将道路图层重投影到 EPSG:3857，输出为新的 GeoPackage
    ↓
Agent 读取必要图层信息和工具契约
    ↓
生成 WRITE 执行计划（不执行）
    ↓
本地校验图层、CRS、参数、输出路径与冲突
    ↓
工作台展示输入、输出、影响、风险和处理算法
    ↓
用户明确确认
    ↓
QGIS Processing 任务执行
    ↓
验证输出文件、可重开性、图层有效性和目标 CRS
    ↓
添加结果图层，提供缩放/属性表等真实后续动作
```

## 5. 模块职责

### Chat UI

只负责展示和交互：会话信息、快捷任务、消息、工具进度、审计记录、计划、确认、取消、错误、结果和设置。不直接操作 PyQGIS，也不根据模型原始文本直接执行操作。

### Application Controller

连接 UI、Agent、模型、权限层、Processing 和 QGIS 生命周期；保证插件关闭时请求、任务和资源正确结束。所有 QGIS 项目、图层、画布、属性表操作由 Controller 在主线程协调。

### Agent Core

负责对话上下文、模型请求、多轮工具调用、最大步数、超时、取消和错误恢复。不得依赖具体 UI，也不得直接调用 PyQGIS。它产生并消费结构化事件，而不是把内部思维链当作 UI 内容。

### Model Adapter、Capability Profile 与 Request Options

- `Model Adapter` 负责请求、解析、认证、超时和协议错误；
- `Model Capability Profile` 申明某类模型接口实际支持的选项；
- `Request Options` 根据档案和用户选择构造请求字段；
- UI 只展示和收集已支持的模式，不能把未经验证的厂商参数当作通用能力；
- API Key 仍由 QGIS Authentication Manager 保存，普通设置只保存非敏感连接和档案信息。

原始模型思维链不属于产品输出。用户可见的是模型/工具/Processing 的结构化状态、输入范围、结果摘要、耗时和审计记录。

### Context Manager

只提供完成当前任务所需的项目、图层、字段、CRS、选择集和处理环境摘要。初始模型上下文保持轻量：项目身份与受限图层发现信息。字段、属性、范围、选择集和空间关系等细节只能按工具调用取得，大规模要素不得未经限制发送给模型。

### Native QGIS Tool Registry

注册经过权限分类、参数校验、数据规模限制和结构化结果约束的高层工具。Agent 只看到稳定的用户任务契约，不看到 QGIS 内部对象，也不直接看到完整 Processing 算法目录。

工具按能力族组织，而不是在单个注册文件中持续堆叠：

- `project/vector diagnostics`：项目、CRS、字段、几何、统计和选择集检查；
- `vector query/processing`：表达式、空间关系、缓冲、重投影、裁剪、导出、相交和融合；
- `raster/dem`：栅格元数据、波段、NoData、分辨率和受控地形分析；Goal 10 已新增独立只读诊断与运行时 provider 探测，尚未开放栅格写入；
- `cartography/reporting`：只消费已验证图层和结构化结果；
- `data maintenance`：原地编辑和删除，使用独立高风险边界；
- `external/developer`：MCP 和代码执行，不进入普通内置工具集合。

每个公开工具除名称、schema 和 handler 外，还必须声明能力族、输入/结果类型、权限、风险等级、数据规模上限和所需 provider。Registry 在发现阶段隐藏当前运行时不支持的工具，不能把 provider 缺失留到确认后才失败。

权限语义：

- `READ_ONLY`：不得改变项目、图层、选择集、样式、布局或源数据；
- `WRITE_NEW_OUTPUT`：仅创建经过确认的新文件、新图层、新样式快照或新布局；现有 `WRITE` 在迁移期等价于该级别；
- `MUTATE_SOURCE`：修改既有数据或项目对象，必须有差异预览、事务/撤销或备份；
- `DESTRUCTIVE`：删除或不可逆操作，独立确认且永不自动执行；
- `CODE_EXECUTION`：普通产品模式不注册。

### Execution Plan 与 Permission Gate

所有 WRITE 工具必须先形成只包含可序列化数据的执行计划：输入、参数、输出、影响、风险、算法、确认状态。计划阶段不得创建文件、加图层、改源数据或保存项目。

用户点击确认是唯一副作用入口。确认后通过 QGIS Processing 执行，且只有输出文件存在、可重开、结果层有效时才加入项目。取消、失败、冲突和无匹配不得显示成功。

### Processing Task

网络和长耗时处理不得阻塞 QGIS：

- 模型 HTTP 请求在网络线程执行，线程只承载普通 Python 数据；
- PyQGIS 读取、图层/地图画布操作和计划构建在 QGIS 主线程；
- 确认后的 Processing 使用 `QgsProcessingAlgRunnerTask` / QGIS Task Manager；
- 调度、阶段状态、取消、审计和结果回传可以共享；
- 参数构造与结果验证必须按矢量、栅格、制图、报告等结果类型分开，禁止用一个万能执行器吞掉不同的不变量；
- 栅格结果至少验证可重开性、CRS、范围、宽高、波段、数据类型和 NoData；矢量结果继续验证图层有效性、字段/要素与源数据不变量；
- 失败或取消只清理当前计划新建的部分输出，绝不按模糊路径清理用户既有文件；
- 不将 `QgsProject`、图层或要素跨入网络线程。

### Diagnostics 与审计记录

日志和用户审计记录分别服务于排查和可解释性：

- 日志记录工具名、状态、耗时和经过脱敏、长度限制的摘要；
- 用户审计记录展示本轮模型、模式、执行阶段、工具轨迹、输出及未执行事项；
- 两者均不得记录 API Key、Authorization、Bearer token、password、无界属性数据或原始思维链。

## 6. 线程、状态与错误恢复

### 6.1 可见状态机

工作台至少区分：

```text
就绪
→ 模型分析
→ 读取项目 / 调用只读工具
→ 等待确认
→ QGIS Processing
→ 完成 | 失败 | 取消
```

状态必须反映真实事件：不能用动画伪造 Processing 百分比，不能用“完成”掩盖取消或失败。若 QGIS Processing 提供真实进度，可显示该进度；否则仅显示真实阶段和取消入口。

### 6.2 错误与恢复

| 问题 | 用户看到的处理 |
|---|---|
| 未配置模型 | 打开设置向导，不进入假聊天 |
| 当前模型不支持行为模式 | 显示服务默认/服务端控制，不伪造支持 |
| API Key 无效 | 显示认证失败，不回显或记录密钥 |
| 网络超时 | 显示当前超时、设置与重试路径，不后台自动重试 |
| 模型返回无效工具参数 | 本地校验拒绝，返回结构化失败并停止/引导修正 |
| 图层不存在或已变化 | 重新读取有限项目状态，拒绝使用失效对象 |
| 只读预览无匹配 | 显示零结果，不改选择集或源数据 |
| 输出文件已存在 | 默认拒绝覆盖，要求新路径 |
| Processing 失败 | 显示真实阶段/错误，不添加伪造结果层 |
| 用户取消 | 清理/不显示成功，提示检查可能残留输出 |
| QGIS 项目未保存 | 提示保存或明确指定输出目录，不擅自保存 |
| 插件关闭/QGIS 退出 | 取消请求和任务并释放资源 |

## 7. 数据与安全边界

- API Key 使用 QGIS Authentication Manager，不写入项目文件、源码、普通日志、聊天记录或测试快照；
- 默认不上传完整图层数据；只发送完成当前任务所需的受限项目元数据、工具结果和有限样本；
- 文件路径、图层属性和项目内容属于潜在敏感信息，必须按工具和模型请求最小化；
- 只读预览不得修改项目、图层、选择集和源数据；
- 写入工具优先输出新文件，默认拒绝覆盖；
- 处理前必须展示影响和风险，用户点击确认才可执行；
- 当前不注册任意代码、删除、原地批量编辑、自动保存或自动执行；
- 外部 MCP 后置，未来若接入需要独立授权、隔离、数据范围和取消机制。

## 8. MCP 的定位

MCP 不是 QGIS Copilot 内置 QGIS 操作的必经链路。当前产品通过内部 Tool Registry 直接调用 PyQGIS，避免额外进程、端口和协议故障点。

后续 MCP 扩展只承担两类职责：

1. 让 QGIS Copilot 在用户授权后连接外部数据和工具服务；
2. 让其他 MCP Host 在用户授权后连接 QGIS Copilot。

MCP 按独立能力族进入，不能因为某个 MCP Server 已暴露大量工具就自动导入。第一阶段只允许一个已登记服务器和一个只读工具；每个服务器必须具备显式启用、工具白名单、发送数据预览、超时、取消、断线恢复和审计。MCP 写入能力需要另行通过对应的写入或破坏性权限门。

## 9. 工具增长与兼容治理

- 每个新工具先提交能力卡，再进入开发计划；能力卡记录任务链、依赖、权限、provider、输入/输出类型、结果不变量和 Desktop 证据；
- 工具 schema 和计划 payload 是 UI、Controller、任务执行器和测试共享的公共契约，变更时必须提供兼容或显式迁移；
- Tool Registry 的发现结果按能力族和运行时支持裁剪，避免模型同时面对大量无关或不可用工具；
- 同义、重叠工具不得同时注册；优先扩展稳定的高层任务契约，不直接镜像算法名称；
- 跨能力工作流只能组合已经分别通过阶段门的工具，不能用编排掩盖单项能力未验收；
- 每个版本设置能力预算：一次只实现一个新能力族的一个垂直切片，阶段通过后再扩同族工具；
- QGIS 版本、provider 或算法参数变化必须通过运行时探测和契约测试发现，不在生产执行时临时猜测；
- UI 入口按用户任务和最近结果组织，不随注册工具数量线性增长。

## 10. 模块、文件与依赖边界

### 10.1 独立逻辑必须独立文件

不同业务模块、逻辑任务和变化原因必须分别放在新的代码文件中。不能因为调用方便，把后续栅格、制图、报告、编辑、MCP 等实现继续堆入现有 `controller.py`、`chat_dock.py`、`qgis_tools.py`、`processing_tools.py` 或 `tasks/processing.py`。

拆分以真实职责为单位，不以代码行数机械切割：

- 一个文件只承担一个可明确命名的主要职责和一种主要变化原因；
- 每个新能力族至少独立拥有工具契约/计划构建、执行任务和结果验证边界；
- 同一能力族内，诊断、写入计划、算法参数构造、结果验证、UI 展示模型分别变化时，必须继续拆成独立文件；
- UI 组件文件只负责展示和信号，Controller 只做流程协调和类型化分派；
- 公共 helper 只有被两个以上真实模块稳定复用时才抽取，不建立空壳或未来假设模块；
- 禁止建立 `utils.py`、`helpers.py`、`common.py` 一类无清晰领域边界的杂物文件；
- 禁止用一个万能执行器、万能参数字典或大型条件分支承载不同能力族；
- 测试按 Goal 和业务行为单开文件，不把所有新能力追加到旧测试文件末尾。

### 10.2 依赖方向

依赖只能沿以下方向流动：

```text
UI -> Application Controller -> Agent / Tool Registry -> Capability Module -> QGIS Task / Validator
```

约束如下：

- UI 不导入 Processing 算法实现，不直接访问或修改 QGIS 项目对象；
- Agent Core 不导入 Qt Widget、QGIS 图层或具体 Processing 任务；
- Capability Module 不反向调用 UI 或 Controller；
- Task 只消费已验证、可序列化的计划，不解析模型自由文本；
- Validator 不负责 UI 展示、工具注册或任务调度；
- 能力族之间不得直接导入彼此内部实现；跨能力组合通过稳定契约或应用层编排；
- 注册模块只汇总 `ToolSpec`，不得包含具体业务算法；
- 出现反向依赖或循环导入时，当前实现停止，不用延迟导入掩盖架构问题。

### 10.3 Goal 1-9 增量保护

v3.0 默认在现有实现旁新增模块，通过最小适配接入。不得先拆散、重命名或重写已验证文件，再依靠后续测试修复。确需迁移旧职责时采用以下顺序：

```text
建立兼容测试 -> 新建目标模块 -> 保留旧入口委托 -> 逐个迁移调用方
-> 运行同一回归与 Desktop 路径 -> 确认无外部引用 -> 最后删除旧实现
```

旧工具名称、schema、计划 payload 和用户可见行为在迁移期间保持不变。兼容层必须有明确移除条件，不能永久形成两套实现；但在证据未完整前，宁可保留已验证入口，也不得提前删除。

### 10.4 目标目录

```text
qgis_copilot/
├── __init__.py
├── metadata.txt
├── plugin.py                    # 薄生命周期入口
├── ui/                          # Qt 展示与信号；按任务卡/对话框拆文件
├── application/                 # 流程协调、类型化分派、结果动作
├── agent/                       # 对话、工具循环、结构化事件
├── models/                      # 模型适配、设置、能力档案
├── tools/
│   ├── contracts.py             # 稳定公共契约
│   ├── registry.py              # 仅注册、发现与分派
│   ├── vector/                  # 矢量诊断、查询和计划模块
│   ├── raster/                  # 栅格诊断、计划、参数和验证模块
│   ├── cartography/             # 样式与布局计划模块
│   ├── reporting/               # 结构化报告计划模块
│   └── maintenance/             # 后续原地编辑/删除；普通模式不注册
├── tasks/
│   ├── network.py
│   ├── vector_processing.py
│   ├── raster_processing.py
│   ├── cartography.py
│   └── reporting.py
├── context/                     # 有界项目/图层上下文
├── security/                    # 凭据、权限、脱敏
└── diagnostics/                 # 日志和审计辅助
```

这是职责边界，不要求 Goal 10 一次迁移现有矢量文件。新目录只在对应真实模块进入实现时创建，避免空目录和投机性抽象。

## 11. 架构验收标准

- 干净 QGIS 环境只安装插件即可打开工作台；
- 无 Node.js、uv、Hermes、WebEngine 或独立后端进程依赖；
- 用户可看懂当前项目、模型、模型行为模式、执行边界和本轮状态；
- 模型能力档案仅暴露已验证的请求语义；
- 只读诊断与预览返回受限、真实结果且不修改 QGIS 状态；
- 每个写入工具都完整经历计划、确认、Processing、输出验证、添加结果与审计反馈；
- 网络与 Processing 不冻结 QGIS；
- 取消、超时、模型错误、参数拒绝、输出冲突与 Processing 错误均不伪造成功；
- API Key 和敏感项目内容不进入公开仓库或普通日志；
- 自动化、真实 Desktop 和干净安装证据必须区分记录，不能互相替代；
- 新能力按独立职责新增文件，入口文件没有吸收新的业务算法或结果验证逻辑；
- 依赖方向无反向调用或循环导入，Registry、Controller、UI 和 Task 各自保持单一职责；
- Goal 1-9 的工具契约、用户流程、自动化与真实 Desktop 代表路径没有回归；
- 任何旧模块迁移均有兼容入口、迁移清单、前后对照证据和明确删除条件。
