# QGIS Copilot 产品需求文档

## 1. 文档状态

- 状态：v2.0 可信 GIS 工作台已完成；v3.0 功能扩展需求与路线图已确认，尚未开始产品代码
- 产品名称：QGIS Copilot
- 产品形态：QGIS 插件内置 AI GIS 助手
- 当前产品方向：按能力族扩展功能，第一主线是栅格/DEM；先建立栅格底座，再做单个可验收垂直切片
- 权威项目目录：本仓库根目录

本文档记录已确认的产品方向、范围、阶段目标与验收口径。产品体验审查提出的过程性判断，只有经确认并写入本文档后才构成实现需求。

## 2. 产品愿景

让 QGIS 用户只需要描述想完成的 GIS 任务，就能在当前 QGIS 项目中获得**可理解、可解释、可确认、可追溯**的分析与执行结果，而不必先学习 MCP、Agent、PyQGIS 或复杂的外部客户端配置。

产品不把 AI 定义为替用户盲目执行一切的自动化器，而是作为能看懂项目、发现问题、提出方案、在确认后完成高频 GIS 任务的协作助手。

## 3. 核心决策

### 3.1 总体架构

QGIS Copilot 采用 **QGIS 插件直接作为 Agent Host** 的产品形态：

- 聊天工作台在 QGIS 内；
- Agent 运行逻辑由产品自身负责；
- QGIS 原生能力优先通过插件内部工具直接调用 PyQGIS；
- MCP 作为外部工具和生态扩展协议，而不是普通用户的必需依赖；
- 不要求普通用户安装 Hermes、Claude Desktop、Cursor、uv 或单独的 Agent 服务。

首发和本轮增量均采用单体 Python 插件：QGIS 插件、Qt Widgets 聊天界面、Agent Core、模型适配和 PyQGIS 工具在同一运行单元中。不得为本轮需求引入 TypeScript、Node.js、React、WebEngine 或独立后端进程。

### 3.2 模型策略与会话可见性

模型层采用提供商抽象，不绑定单一厂商：

- 当前已支持 OpenAI-compatible HTTP 接口；
- 本地模型服务和其他提供商可作为后续扩展；
- 插件安装不应强制用户安装本地模型或承担固定云端费用；
- API Key 仅通过 QGIS Authentication Manager 保存，不进入普通设置、日志或聊天记录。

聊天工作台必须向用户清楚展示当前会话的模型名、接口类型、连接状态、模型行为模式、执行模式和项目摘要。

“思考强度”不得作为无实际请求语义的装饰开关。产品采用**模型能力档案（Model Capability Profile）**：

- 所有模型至少支持“服务默认”；
- 仅当档案与实际模型接口明确支持时，才开放快速、平衡、深度等模型行为模式，并发送对应请求参数；
- 不支持可调推理参数时，界面明确显示“推理：服务默认”或“由模型服务控制”；
- 不展示或伪造模型原始思维链。

### 3.3 用户安全策略

默认采用“先理解、后计划、再确认、后验证”：

- 查询、检查、统计、预览等只读操作可以直接执行；
- 创建结果文件、处理空间数据、导出结果等写入操作必须先展示计划并请求明确确认；
- 首发提供只读模式和确认执行模式；自动执行模式不属于本轮范围；
- 任意 PyQGIS 代码执行默认关闭，不属于普通用户能力；
- 每次工具和 Processing 执行必须有用户可见的结构化过程与结果记录；
- 写入优先生成新文件/新图层，默认拒绝覆盖已有输出。

### 3.4 当前增量优先级

本轮不进行“工具数量竞赛”，按真实任务链补齐能力：

```text
看懂数据 → 发现问题 → 明确目标 → 形成方案 → 确认执行 → 验证结果 → 继续迭代
```

- **数据检查与分析**是优先主线，负责让 Copilot 看懂项目并发现风险；
- **空间处理**与其并行推进，但只实现能承接诊断结论的高频、低歧义、可验证操作；
- 聊天工作台的会话信息、过程透明度与结果操作属于所有能力的共同前置体验。

## 4. 目标用户流程

### 4.1 首次使用

1. 用户通过 QGIS 插件管理器安装并启用 QGIS Copilot；
2. 在 QGIS 工具栏或菜单点击“QGIS Copilot”；
3. 插件打开可停靠的聊天工作台；
4. 首次使用时用户选择模型来源并完成必要配置；
5. 插件显示当前项目、模型连接、执行模式与数据发送边界；
6. 用户通过快捷任务或自然语言提出目标。

### 4.2 数据检查与分析任务

1. 用户提出“检查项目是否适合空间分析”等自然语言请求；
2. Agent 读取有限的项目与图层摘要；
3. Agent 调用项目诊断、CRS 一致性、图层质量、字段统计、表达式/空间关系预览等只读工具；
4. 聊天工作台展示自然语言结论、问题明细、使用的数据范围和可展开的执行轨迹；
5. 用户可以定位相关图层、查看详情，或直接选择由诊断引出的安全处理建议。

### 4.3 受控空间处理任务

1. 用户提出重投影、裁剪、筛选导出、相交等任务，或从诊断建议进入处理；
2. Agent 读取必要图层与参数，生成而非直接执行写入计划；
3. 插件本地校验输入图层、CRS、参数、输出路径、文件冲突与预期影响；
4. 聊天工作台以计划卡展示输入、参数、输出、影响、风险与处理算法；
5. 用户确认或取消；
6. 确认后通过 QGIS Processing 执行；
7. 插件验证输出文件可重开、结果图层有效，再添加到当前项目；
8. 聊天工作台展示真实结果，并提供定位图层、打开属性表或继续分析等后续动作。

### 4.4 错误、取消与恢复

- 未配置模型时，明确引导至设置，不伪造聊天结果；
- 网络超时、认证失败、模型参数无效、工具失败、Processing 失败和用户取消均显示真实状态；
- 不进行后台静默重试；
- 取消和失败不得显示为成功；
- 失败结果应提供可理解的下一步，例如调整设置、修改计划、重试或查看详情。

## 5. 聊天工作台与 UX 要求

### 5.1 会话信息条

Dock 顶部必须展示紧凑、可读且真实的会话信息：

- 当前项目名称、保存状态与图层数量；
- 当前模型名、接口类型和连接状态；
- 模型行为模式：服务默认或由模型能力档案支持的模式；
- 当前执行模式：只读或确认执行；
- 设置与项目摘要入口。

信息不得由猜测或硬编码生成。模型服务不支持的能力必须禁用或明确说明，不得伪造已启用效果。

### 5.2 任务入口与空状态

首次打开、无对话或清空会话时，界面应清楚说明已可用能力并提供可编辑的快捷任务：

- 检查项目健康状态；
- 查看图层与坐标系；
- 分析字段数据；
- 按条件筛选要素；
- 进行空间关系分析；
- 处理或导出数据。

快捷任务只帮助用户形成请求，不应在未发起请求或未确认写入前产生副作用。

### 5.3 工具执行、审计与结果卡

每轮任务至少可见：

- 当前阶段：模型分析、读取项目、调用工具、等待确认、QGIS Processing、完成、失败或取消；
- 已调用工具名、简短结果摘要、耗时与是否修改项目；
- 可展开的输入参数、返回摘要、错误详情和数据范围；
- 写入任务的计划、确认、取消及验证结果；
- 本轮审计记录：模型、模式、工具轨迹、输出与未执行事项。

默认展示自然语言结论和必要摘要；原始 JSON 只能作为可展开的高级信息。不得显示原始思维链，不得模拟不存在的处理百分比。

### 5.4 结果后的 QGIS 原生操作

当对应状态真实可用时，结果卡应提供：

- 图层类结果：定位图层、缩放至图层、打开属性表；
- 诊断类结果：查看问题详情、定位受影响图层、生成推荐处理计划；
- 筛选预览：在地图中高亮或打开属性表；
- 统计结果：复制表格、继续分析；
- 输出结果：定位图层、打开属性表、查看输出路径；
- 错误结果：查看详情、调整计划或重试。

按钮必须由真实 QGIS 状态驱动；不可用时隐藏或禁用，不能提供无效果入口。

### 5.5 本次 QGIS 打开期间的会话历史

- 用户点击“新建会话”时，当前会话不能被销毁；它应保留在本次 QGIS Desktop 运行的内存会话列表中，并以“会话 1”“会话 2”等可辨识条目切换查看；
- 每个会话独立保留可见消息、工具卡、审计记录、Agent 对话上下文与待确认计划；切换会话不得更改 QGIS 项目、图层、选择集或源数据；
- 运行中的网络请求或 Processing 不允许切换/新建会话，以免将实际副作用与错误会话混淆；
- 该历史仅存在内存：关闭 QGIS Desktop 后必须全部清除，绝不写入 QSettings、项目文件、普通日志、ZIP、API Key 存储或任何聊天持久化文件。

## 6. 当前阶段范围

### 6.1 已完成的 MVP 基线

- QGIS 原生聊天 Dock、模型配置、OpenAI-compatible 非流式请求与安全凭据存储；
- 有界项目/图层只读上下文和工具调用闭环；
- `get_project_state`、`list_layers`、`inspect_layer`、`query_features`；
- 可确认的 `buffer_vector` 写入计划与 QGIS Processing 验证闭环；
- ZIP 安装、基础日志脱敏和 Windows + QGIS 4.2.1 的真实用户路径验证。

### 6.2 本轮必须实现：工作台透明度

- 会话信息条与真实模型、项目、执行状态；
- 模型能力档案与受控模型行为模式；
- 更新空状态和可编辑快捷任务；
- 统一的工具执行卡、计划卡、结果卡和本轮审计记录；
- 清晰区分模型请求、只读工具、等待确认、Processing、成功、失败和取消；
- 可用结果的 QGIS 原生后续操作。

### 6.3 本轮必须实现：数据检查与分析

以下能力均为 `READ_ONLY`，必须限定数据规模、返回量和错误边界：

1. `get_project_diagnostics`：失效图层、空图层、未保存编辑、项目/图层 CRS 风险；
2. `check_crs_consistency`：项目或指定图层的 CRS 一致性、风险和建议；
3. `validate_layer`：矢量图层的字段、空几何、无效几何等质量检查；栅格图层返回适用的安全元数据检查；
4. `get_layer_statistics`：字段空值、唯一值和受限数值统计；
5. `validate_expression` / `select_by_expression_preview`：只验证表达式、预估命中数量与有限样本，不修改选择集；
6. `spatial_query_preview`：预览相交、包含、邻近等空间关系的数量和有限样本，不修改源数据或选择集；
7. 当前选择集摘要：读取当前选择集的有限统计与属性摘要，不改变选择状态。

### 6.4 本轮必须实现：受控空间处理

以下能力均为 `WRITE`，必须遵循计划 → 明确确认 → QGIS Processing → 输出验证 → 加入项目：

1. `reproject_layer`：重投影到目标 CRS，输出新的结果图层；
2. `clip_vector`：以矢量掩膜裁剪矢量输入，输出新的结果图层；
3. `export_filtered_features`：按已验证表达式、当前选择集或已验证空间条件导出新的 GeoPackage；
4. `intersection`：对明确输入图层进行矢量相交并输出新图层；
5. `dissolve`：按用户明确的字段/规则融合并输出新图层。

`buffer_vector` 继续保留并收敛到同一计划、执行、验证与结果卡框架。

## 7. 分阶段交付与验收

### 阶段 A：可信会话基础与项目入口

**目标：** 用户在十秒内理解当前项目、模型连接、可用能力和写入安全边界。

**验收：**

- 会话信息条反映真实设置和项目状态；
- 无对话状态不再出现已过时的 Goal 占位文案；
- 快捷任务可填入且可编辑，不自动执行；
- 工具、计划、Processing、错误、取消的状态可见且不混淆；
- 不显示伪造模型能力、原始思维链或虚假进度；
- 每轮可展开查看审计记录。

### 阶段 B：数据检查与重投影闭环

**目标：** 用户能检查项目是否适合分析，并安全解决高频 CRS 问题。

**验收：**

- 项目诊断、CRS 一致性、图层质量、字段统计和表达式预览返回与 QGIS 状态一致的受限结果；
- 初始模型上下文不因新增功能上传完整属性表或无界数据；
- 诊断结论能引导用户创建重投影计划；
- 重投影不会覆盖输入数据，输出在确认后生成、可重开、CRS 正确并加入项目；
- 失败、取消或输出冲突不产生伪造成功。

### 阶段 C：空间关系预览、裁剪与筛选导出闭环

**目标：** 用户能从问题判断进入基础数据整理并得到可复核的新结果。

**验收：**

- 空间关系和筛选预览不会更改项目、选择集或源数据；
- 预览明确区分几何裁剪、属性筛选和空间条件；
- 裁剪与筛选导出均展示输入、条件、输出、影响和风险；
- 确认后真实生成新 GeoPackage，输出可加载并可执行后续 QGIS 操作；
- 默认拒绝覆盖，源图层保持不变。

### 阶段 D：高频叠加分析与结果表达

**目标：** 用户能完成常见矢量叠加分析，并以可理解的方式查看结果。

**验收：**

- 相交与融合遵循统一安全写入闭环；
- 字段保留、分类字段、CRS、输出路径和风险在确认前可见；
- 统计和结果卡支持复制/继续分析等真实可用的后续动作；
- 真实 QGIS Desktop 路径覆盖成功、取消、参数拒绝和 Processing 失败。

## 8. 功能扩展策略与能力地图

### 8.1 不按工具箱数量开发

QGIS Processing 工具很多，但 Copilot 不按工具名称逐个搬运。新功能必须先归入一个明确的用户任务链和能力族，再判断是否复用现有契约、计划、执行、验证与结果表达。没有任务关系、权限边界或验收方法的工具不得注册给模型。

功能排序同时考虑：

1. 用户价值和使用频率；
2. 与已完成能力的前后依赖；
3. 是否具备可复用的参数语义与结果验证；
4. 数据破坏、隐私、性能和兼容性风险；
5. UI 是否仍能让用户理解当前能力，而不是堆满工具入口；
6. 自动化与真实 QGIS Desktop 是否能够提供可观察证据。

### 8.2 能力族

| 能力族 | 典型能力 | 依赖关系 | 当前定位 |
|---|---|---|---|
| 项目与数据诊断 | CRS、字段、几何、栅格元数据、处理环境 | 所有处理能力的前置 | 已有矢量基线，继续扩展栅格诊断 |
| 矢量查询与处理 | 筛选、裁剪、相交、融合、缓冲、重投影 | 诊断、计划确认、输出验证 | v2.0 已完成基线 |
| 栅格与 DEM | 栅格检查、裁剪、重投影、坡度、坡向、等高线、分区统计 | 栅格诊断、单位/NoData/分辨率语义、长任务执行 | v3.0 第一主线 |
| 地图表达 | 符号化、标签、图例、布局、导出 | 稳定分析结果、样式预览和回滚 | 栅格切片稳定后分场景进入 |
| 报告交付 | 结构化结论、统计表、地图、HTML/PDF | 可追溯分析结果和稳定地图输出 | 制图与结构化结果稳定后进入 |
| 数据维护 | 字段更新、批量编辑、删除 | 变更预览、事务、撤销、备份、冲突检测 | 后置，高风险独立路线 |
| 工作流自动化 | 重复任务模板、批处理、自动执行 | 稳定工具链、权限策略、失败恢复和审计 | 后置，仅允许受限工作流 |
| 外部扩展 | MCP 外部数据/工具、其他 Host 调用 QGIS | 授权、隔离、数据范围、连接管理 | 可选扩展，不是内置能力前置 |
| 高级开发 | 任意 PyQGIS/Python 代码 | 沙箱、系统权限、来源可信度 | 普通模式不开放 |

### 8.3 新功能准入门

每个候选功能在进入开发短计划前，必须形成一张能力卡并通过以下检查：

- **任务定义：** 用户输入、预期结果、与前后能力的关系清楚；
- **工具契约：** 参数、默认值、单位、CRS、数据规模、返回结构和错误语义明确；
- **权限分类：** `READ_ONLY`、`WRITE_NEW_OUTPUT`、`MUTATE_SOURCE`、`DESTRUCTIVE`、`CODE_EXECUTION` 之一，不得用普通 WRITE 掩盖更高风险；
- **副作用范围：** 会创建、修改、删除什么，是否改变选择集、样式、布局、项目或源文件；
- **执行方式：** 主线程、QGIS Task Manager、Processing provider 和取消语义明确；
- **结果验证：** 不只检查文件存在，还要定义 CRS、范围、数量、波段、字段、样式或报告内容等结果不变量；
- **兼容性：** 目标 QGIS 版本、算法 ID、provider 和可选依赖已经实机探测；
- **用户界面：** 工具通过自然语言和任务入口使用，不为每个算法新增永久按钮；
- **证据计划：** 有独立自动化、真实 Desktop 成功/取消/失败路径和回归集合；
- **退出方案：** 失败和取消不会污染项目，部分输出、临时文件和锁定资源有清理规则。

任何一项不清楚时，状态只能是“候选”或“阻塞”，不能靠模型提示词弥补未定义的产品契约。

### 8.4 Goal 1-9 不可破坏兼容基线

Goal 1-9 和 v0.2.0 已经完成自动化、打包及真实 QGIS Desktop 验收，是后续开发必须保护的业务基线。v3.0 的架构设计和功能实现只能增量扩展，不能以“统一”“重构”或“清理旧代码”为由改变已经验证的行为。

以下内容默认冻结，除非哥哥单独批准兼容迁移：

- 已公开的工具名称、参数 schema、权限语义和结构化错误；
- OpenAI-compatible `tool_calls`、`tool_call_id` 配对和有界多轮协议；
- 只读工具不改变项目、图层、选择集和源数据的保证；
- 写入工具的计划、确认、取消、Processing、输出验证和加入项目流程；
- 默认拒绝覆盖、源数据不变、失败/取消不显示成功；
- QGIS Authentication Manager 凭据语义、日志脱敏和数据最小化；
- 会话、工作台状态、计划卡、结果卡和真实 QGIS 结果动作；
- 已验证的缓冲、重投影、裁剪、筛选导出、相交和融合业务行为。

新增能力必须满足：

1. 优先新增模块和适配接缝，不先搬迁或重写稳定代码；
2. 旧契约需要演进时，先保留兼容入口，再逐项迁移调用方和测试；
3. 每次只迁移一个明确职责，迁移前后使用同一测试和 Desktop 路径比较；
4. 旧实现只有在全部调用方迁移、回归通过且无真实使用依赖后才能删除；
5. 任一 Goal 1-9 自动化或真实业务路径回归，当前新 Goal 立即失败并停止扩展；
6. 不能通过修改旧测试预期、降低断言或删除测试来把行为变化解释为通过。

### 8.5 扩展顺序

采用“共享底座 → 一个垂直切片 → 同族扩展 → 跨族组合”的顺序：

1. 先补该能力族共用的诊断、数据模型、验证器和任务执行边界；
2. 选择一个高价值、低歧义、可真实验收的功能贯穿工具注册到结果动作；
3. 通过自动化和 Desktop 阶段门后，才加入第二个同族工具；
4. 只有单族能力稳定后，才允许制图、报告、自动化等跨能力编排；
5. 每次只让一个新能力族进入实现状态，其他能力保持候选，不并行扩散架构。

### 8.6 长期安全边界

以下不是永久否定，但必须使用不同准入级别：

- 栅格/DEM：可进入近期开发，但先做元数据诊断和一个完整 DEM 垂直切片；
- 自动制图：按明确场景进入，默认新建样式/布局，不直接覆盖用户现有样式；
- 报告生成：只能引用结构化、可追溯的工具结果，不允许把模型自由文本当作事实来源；
- 原地批量编辑：必须先预览差异、检测活动编辑、支持事务/撤销或备份，并二次确认；
- 删除：使用 `DESTRUCTIVE` 权限，独立确认，不与普通写入共用默认确认；
- 自动执行：只允许用户预先授权的受限工作流；删除、覆盖、任意代码和权限升级永不自动执行；
- 任意代码：普通产品模式不注册；未来开发者模式也必须独立开关、显式警告和来源限制；
- MCP：仅作为可选扩展，必须有逐服务器授权、工具白名单、数据范围、超时、取消和审计；
- 自动覆盖、自动保存项目、云端同步和在线账户仍不进入当前计划。

## 9. 通用质量与安全验收标准

### 聊天、上下文与过程透明度

- 用户能打开、关闭和重新打开聊天工作台；
- 用户能用中文自然语言提出常见 GIS 请求；
- 用户能理解当前模型、项目、执行模式和本轮过程；
- Agent 能识别当前项目中的图层名称、类型和基本状态；
- 工具调用过程、数据范围、结果与错误可理解且可追溯；
- 不展示未被实际支持的模型能力或不可验证的内部推理内容。

### 工具执行

- 只读任务不修改项目、图层、选择集和源数据；
- 写入任务执行前显示目标图层、条件、输出路径、预期影响和风险；
- 用户取消后不执行对应写入操作；
- 成功结果能被 QGIS 识别并按约定添加到当前项目；
- 失败时显示真实错误，不伪造成功；
- 所有输出文件默认不覆盖已有路径，且需重新打开验证。

### 安装、配置与隐私

- 用户不需要手动配置 MCP Client；
- 用户不需要单独安装 Hermes、uv 或 Node.js 才能使用内置功能；
- 配置缺失时有明确提示和恢复路径；
- API Key 不以明文写入公开项目文件、日志或聊天记录；
- 项目路径、图层属性和数据源只在完成当前任务所必需的范围内被读取或发送；
- 支持矩阵仅描述实际验证过的 QGIS 版本和操作系统。
