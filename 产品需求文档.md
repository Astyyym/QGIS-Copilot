# QGIS Copilot 产品需求文档

## 1. 文档状态

- 状态：最终架构基线，MVP 业务细节可在实现阶段细化
- 产品名称：QGIS Copilot
- 产品形态：QGIS 插件内置 AI GIS 助手
- 当前阶段：产品定义与架构准备

本文档记录已经确定的方向，同时明确尚未锁定的内容。未确认事项不能直接当作实现要求。

## 2. 产品愿景

让 QGIS 用户只需要描述想完成的 GIS 任务，就能在当前 QGIS 项目中获得可解释、可确认、可追溯的执行结果，而不必先学习 MCP、Agent、PyQGIS 或复杂的外部客户端配置。

## 3. 核心决策

### 3.1 总体架构

QGIS Copilot 采用 **QGIS 插件直接作为 MCP Host** 的产品形态：

- 聊天界面在 QGIS 内；
- Agent 运行逻辑由产品自身负责；
- QGIS 原生能力优先通过插件内部工具直接调用 PyQGIS；
- MCP 作为外部工具和生态扩展协议，而不是普通用户的必需依赖；
- 不要求普通用户安装 Hermes、Claude Desktop、Cursor、uv 或单独的 Agent 服务。

首发采用单体 Python 插件：QGIS 插件、Qt Widgets 聊天界面、Agent Core、模型适配和 PyQGIS 工具在同一运行单元中。首发不引入 TypeScript、Node.js、React 或独立后端进程。

这是首发的明确技术决定，不是暂时占位。未来只有在外部 MCP 生态、独立 Agent 运行或多端复用形成真实需求后，才允许把 Agent Core 拆为独立服务。

### 3.2 模型策略

模型层采用提供商抽象，不绑定单一厂商：

- 首选支持 OpenAI-compatible API；
- 可选支持本地模型服务，例如 Ollama；
- 后续可以增加其他模型提供商；
- 插件安装不应强制用户安装本地模型或承担固定云端费用。
- 首发只实现 OpenAI-compatible HTTP 接口；本地模型服务作为后续扩展。

### 3.3 用户安全策略

默认采用“先计划、后执行”：

- 查询和只读操作可以直接执行；
- 修改数据、删除内容、覆盖文件、保存项目和执行代码等操作必须明确展示影响并请求确认；
- 支持只读模式、确认模式和自动执行模式；
- 任意代码执行默认关闭或限制在高级模式；
- 所有高风险操作应有可观察的操作记录。

## 4. 目标用户流程

1. 用户通过 QGIS 插件管理器安装 QGIS Copilot；
2. 在 QGIS 工具栏或菜单点击“AI 助手”；
3. 插件打开可停靠的聊天面板；
4. 首次使用时选择模型来源并完成必要配置；
5. 用户输入自然语言任务；
6. Agent 读取必要的项目和图层上下文；
7. Agent 展示准备执行的步骤、输入、输出和风险；
8. 用户确认或取消；
9. QGIS Copilot 执行工具并将结果添加或反馈到当前项目；
10. 聊天记录展示执行结果、错误、输出路径和可验证信息。

## 5. MVP 范围

### 必须实现

- QGIS 插件安装后可启用；
- QGIS 内置聊天面板；
- 模型提供商配置；
- 当前项目和图层的只读上下文读取；
- Agent 多轮工具调用；
- 常见 QGIS 工具调用；
- 执行前计划与确认；
- 工具执行结果和错误反馈；
- 只读、确认、自动三种权限模式中的至少两种；
- 基础日志和诊断信息；
- 不依赖用户手动安装 Python 包或编辑 MCP 配置。

### MVP 工具优先级

1. 获取当前项目信息；
2. 列出图层和图层树；
3. 检查图层字段、CRS、范围和数据源；
4. 查询属性和统计信息；
5. 加载矢量或栅格图层；
6. 执行常见 Processing 算法；
7. 导出结果；
8. 保存项目；
9. 生成地图预览或结果报告。

## 6. 明确不纳入 MVP

- 强制绑定某个模型厂商；
- 强制依赖 Hermes 或其他外部 Agent；
- 一开始覆盖所有 QGIS Processing 算法；
- 默认开放任意 PyQGIS 代码执行；
- 在线账户系统和云端对话同步；
- 自动替用户承担模型 API 费用；
- 在没有确认机制的情况下执行不可逆批量修改；
- 先做复杂网页前端而不验证 QGIS 环境兼容性。

## 7. 功能验收标准

### 聊天与上下文

- 用户能在 QGIS 内打开、关闭和重新打开聊天面板；
- Agent 能识别当前项目中的图层名称、类型和基本状态；
- 用户能用中文自然语言提出常见 GIS 请求；
- 工具调用过程和结果对用户可理解。

### 工具执行

- 只读任务不会修改源数据；
- 修改任务执行前会显示目标图层、筛选条件、输出路径和预期影响；
- 用户取消后不执行对应写入操作；
- 成功结果能被 QGIS 识别，并按约定添加到当前项目；
- 失败时能显示真实错误，不伪造成功。

### 安装与配置

- 用户不需要手动配置 MCP Client；
- 用户不需要单独安装 Hermes 或 uv 才能使用内置功能；
- 配置缺失时有明确提示和恢复路径；
- API Key 不以明文写入公开项目文件、日志或聊天记录。

## 8. 已确定的实现基线

- 首发插件使用 Python + qgis.PyQt Qt Widgets；
- 首发 Agent Core 使用 Python，与 QGIS 工具同进程运行；
- 首发模型接口为 OpenAI-compatible HTTP API；
- 首发不依赖 Node.js、TypeScript、React、Hermes、uv 或独立后端；
- QGIS 原生工具通过内部 Tool Registry 直接调用 PyQGIS；
- MCP 只作为后续外部工具和互操作扩展；
- 首发默认提供只读模式和确认执行模式；
- 任意 PyQGIS 代码执行不属于普通模式能力；
- 首发支持矩阵以实际验证过的 QGIS 版本和操作系统为准，未验证的平台不能写入兼容承诺；
- 许可证在正式发布前确定为与 QGIS 插件分发和依赖兼容的 GPL 许可；
- 首发界面以中文为主，并为后续国际化保留文案边界；
- 首发写入任务优先输出新文件，不覆盖原始数据。
