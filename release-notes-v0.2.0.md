# QGIS Copilot v0.2.0

可信 GIS 工作台增量版本，完成 Goal 1–9 的产品范围、自动化回归、打包与真实 QGIS Desktop 验收。

## 主要能力

- 透明会话工作台：项目、图层、模型、执行边界、工具卡、计划卡和审计记录；
- OpenAI-compatible 非流式模型连接与能力档案；
- 受限只读项目诊断、CRS 检查、图层质量、字段统计、表达式预览、空间关系预览和选择集摘要；
- 统一的计划—确认—Processing—输出验证闭环；
- 新输出 GeoPackage 的缓冲、重投影、裁剪、筛选导出、相交和融合；
- 相交支持明确字段保留与叠加字段前缀；
- 融合要求明确分类字段或全量融合，不自动猜测；
- 结果图层可验证、可重开、可加入项目，源图层不原地修改；
- API Key 仅存储在 QGIS Authentication Manager 中，普通设置和日志不保存密钥。

## 安装

1. 下载本页的 `qgis_copilot.zip`；
2. 在 QGIS 中打开“插件 → 管理并安装插件 → 从 ZIP 安装”；
3. 启用 QGIS Copilot；
4. 打开 QGIS Copilot 工作台；
5. 在设置中填写你信任的 OpenAI-compatible API 地址、模型名和 API Key。

首次保存 API Key 时，QGIS 可能要求设置或输入认证主密码。该主密码由用户自行设置，用于保护 QGIS 认证存储，不是模型 API Key。

## 安全边界

- 写入操作必须经过用户确认；
- 默认拒绝覆盖已有输出；
- 不提供任意 PyQGIS 代码执行、自动执行、删除、原地批量编辑或自动保存项目；
- 不发送无界完整属性数据；
- 当前只在 Windows + QGIS 4.2.1 验证，其他版本和平台需要独立测试。

## 验证摘要

- Goal 9 专项 QGIS 测试：4/4；
- Goal 4/6/7/8 隔离回归：9/9、8/8、4/4、3/3；
- QGIS bundled Python 编译检查通过；
- ZIP 只有一个顶层 `qgis_copilot/` 目录，不含测试、参考仓库、缓存、字节码或密钥文件；
- 相交和融合已完成真实 QGIS Desktop 用户路径验收。
