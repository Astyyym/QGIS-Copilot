# QGIS Copilot v3.0 功能扩展路线图

> 状态：需求与执行规划基线，尚未开始产品代码实现
> 前置：v2.0 Goal 1-9 已完成
> 总方向：按能力族增加功能，不按 QGIS 工具箱数量堆工具

## 1. 总目标

在保持现有“诊断/预览 -> 计划 -> 人工确认 -> Processing -> 结果验证 -> 加入项目”闭环的基础上，建立可持续扩展机制，并交付栅格/DEM、地图表达和报告的分阶段能力。原地编辑、删除、自动执行、任意代码和 MCP 使用独立安全路线，不与普通 Processing 功能混做。

## 2. 功能开发固定动作

每个新功能必须按以下顺序执行：

1. **候选登记**：说明用户任务、输入、输出、前后依赖和不做内容；
2. **能力卡评审**：锁定工具契约、权限、数据规模、算法/provider、线程、取消、清理和结果不变量；
3. **运行时探测**：在目标 QGIS 中核对算法 ID、参数、provider、输出格式及可选依赖；
4. **最小测试数据**：建立虚构或公开的小型数据，包含成功、空结果、非法参数和取消场景；
5. **计划阶段实现**：参数校验和计划卡先完成，证明零副作用；
6. **确认后执行**：接入 QGIS Task Manager/Processing，不从模型文本直接调用算法；
7. **结果验证**：按能力类型验证内容，不以“文件存在”代替正确性；
8. **结果动作**：只暴露真实可用的缩放、属性表、样式、布局或输出入口；
9. **隔离回归**：运行该能力族测试和既有核心回归；
10. **Desktop 验收**：分别验证成功、取消、参数拒绝/失败和源数据不变；
11. **文档同步**：更新能力表、限制、证据和任务状态；
12. **阶段门**：当前垂直切片通过后，才能增加同族下一个工具。

## 3. 通用开发边界

### 3.1 Goal 1-9 兼容合同

- Goal 1-9 和 v0.2.0 是已验证业务基线；v3 只能增量扩展，默认禁止重写、重命名、搬迁或删除稳定实现；
- 不改变旧工具名称、schema、plan payload、权限、错误语义和用户可见流程；
- 现有 `WRITE` 保持可用，新增权限层通过兼容映射接入，不直接修改旧枚举值或旧分派结果；
- Goal 10 优先在旧实现旁增加能力元数据和栅格模块，不把旧矢量文件一次性迁入新目录；
- 必须修改旧文件时，执行前在任务记录中列出准确文件、修改原因、旧行为不变量、影响调用方和对应回归；
- 一次只修改一个公共接缝；不得同时改 Registry、计划 payload、Controller 分派和 UI 契约后再统一排错；
- 旧逻辑迁移遵循“兼容测试 -> 新文件 -> 旧入口委托 -> 逐个迁移 -> 回归 -> 最后删除”，不得直接替换；
- 禁止删除旧测试、降低断言、改写成功标准或用新测试覆盖旧失败；
- 任一旧 Goal 回归时，当前 Goal 状态立即标为失败，停止新增功能并恢复兼容，不带病进入下一阶段。

### 3.2 模块与文件合同

- 不同业务模块、逻辑任务和变化原因分别单开新的代码文件；
- 新栅格能力不得继续写入现有矢量 `processing_tools.py` 或 `tasks/processing.py`；
- 栅格诊断、计划构建、provider 探测、算法参数构造、执行任务、结果验证分别具有独立职责；职责发生独立变化时必须独立文件；
- Controller 只协调和类型化分派，不能包含栅格/矢量算法参数或结果验证；
- UI 主 Dock 只组合组件和转发信号；新计划卡或结果展示形成独立组件文件，不继续扩充大段条件渲染；
- Registry 只注册、发现和分派；具体能力规则放在能力模块；
- 测试按 Goal 与业务行为单开文件；公共夹具只有真实复用后才抽取；
- 不创建无领域含义的 `utils.py`、`helpers.py`、`common.py`，不使用万能执行器或万能参数字典；
- 不机械规定一个函数一个文件；拆分标准是职责、依赖和变化原因，而不是行数。

### 3.3 运行与安全边界

- 保持 Python + PyQGIS + qgis.PyQt Qt Widgets 单体插件；
- UI 不直接执行算法，Agent Core 不直接接触 QGIS 对象；
- 工具注册表只公开经过产品化的高层任务，不公开整个 Processing 算法目录；
- 新功能不得依赖模型猜测 CRS、单位、字段、波段、输出范围或覆盖意图；
- 长任务必须可取消；取消和失败不得加入伪造结果层；
- 新输出默认拒绝覆盖，临时文件和部分输出必须定向清理；
- 每个能力族拥有自己的参数校验和结果验证器，共享调度与审计，不用一个万能参数字典吞掉类型差异；
- 不为每个工具增加永久按钮；入口按“检查数据、分析地形、制作地图、生成报告、维护数据”等用户任务组织；
- 不上传完整栅格、完整属性表、项目文件或无界数据给模型；
- 真实 QGIS Desktop 是阶段门，headless 测试不能替代。

### 3.4 每阶段固定回归门

每个 v3 Goal 至少执行并分别记录：

1. Goal 1-3：插件生命周期、模型工具协议、只读上下文与工具闭环；
2. Goal 4-5：计划确认、取消、Processing、安全凭据、日志脱敏与打包边界；
3. Goal 6：工作台状态、计划/结果卡、会话和真实结果动作；
4. Goal 7-9：诊断、重投影、空间预览、裁剪、筛选导出、相交和融合；
5. 当前 Goal 专项测试；
6. QGIS bundled runtime 编译/导入；
7. 真实 Desktop 旧代表路径：一个只读工具、一个矢量写入计划取消、一个矢量确认执行和一个结果动作；
8. 当前新能力的成功、取消、拒绝/失败路径。

QGIS 测试因原生退出问题需要隔离进程时，继续使用独立 exit 0 结果，不能用测试正文通过掩盖非零退出。

## 4. Goal 10：能力治理与栅格基础底座

### 目标

先解决工具增长冲突，再加入任何栅格写入能力；建立栅格可用性诊断、类型化计划和类型化结果验证边界。

### 准确文件

**优先新增文件：**

- `qgis_copilot/tools/raster/diagnostics.py`
- `qgis_copilot/tools/raster/provider_probe.py`
- `qgis_copilot/tools/raster/plans.py`
- `qgis_copilot/tools/raster/validators.py`
- `qgis_copilot/tasks/raster_processing.py`
- `qgis_copilot/ui/raster_plan_card.py`（只有栅格计划卡真实需要专用展示时新增）
- `tests/test_goal10_raster_foundation.py`

**允许最小适配的既有文件：**

- `qgis_copilot/tools/contracts.py`：只增加向后兼容的元数据/权限表达；
- `qgis_copilot/tools/registry.py`：只接入能力发现，不放栅格业务逻辑；
- `qgis_copilot/application/controller.py`：只增加类型化分派，不放算法参数或验证；
- `qgis_copilot/ui/chat_dock.py`、`ui/view_models.py`：只组合新组件或接入新状态，不内嵌栅格规则。

Goal 10 不迁移、不重命名、不删除现有矢量工具或任务文件。若上述既有文件需要超出最小适配，立即停止并先更新计划。

### 工作内容

- 将权限语义明确为只读、新建输出、修改源数据、破坏性、代码执行；保持旧 WRITE 契约兼容；
- 为工具增加能力族、风险级别、支持的数据类型和结果类型元数据；
- 实现栅格只读诊断：provider、CRS、范围、宽高、像元大小、波段、数据类型、NoData、有限统计和 provider 可用性；
- 探测目标 QGIS 的 GDAL/native 栅格算法，不把未安装 provider 暴露给模型；
- 建立 `RasterExecutionPlan` 或等价类型化契约，表达输入波段、单位、分辨率、范围、NoData、输出格式和预计磁盘影响；
- 建立栅格验证器：文件可重开、图层有效、CRS、范围、宽高、波段数、数据类型和 NoData 符合计划；
- 计划卡针对栅格显示专用参数，不复用矢量字段文案。

### 验收

- 栅格诊断完全只读且返回有界结果；
- 缺失 CRS、无效波段、未知 NoData、provider 不可用均结构化失败；
- 栅格计划对象可展示但不会创建文件或改变项目；
- 旧矢量工具和计划卡行为不变；
- QGIS bundled runtime 自动化 exit 0，并在 Desktop 核对真实 DEM 元数据。

### 停止条件

- 目标 QGIS 的算法/provider 无法稳定探测；
- 需要把 QGIS 对象传入网络线程；
- 需要用通用任意参数或任意算法执行绕开类型化契约；
- 栅格计划无法表达单位、分辨率或 NoData 风险。

## 5. Goal 11：DEM 地形分析最小垂直切片

### 目标

交付一个完整、可验证的 DEM 地形分析链，第一项固定为坡度分析；它通过后再加入坡向和等高线。

### 准确文件

- `qgis_copilot/tools/raster/dem_plans.py`（新增，坡度/坡向/等高线计划契约）
- `qgis_copilot/tools/raster/dem_parameters.py`（新增，单位、Z factor 与算法参数）
- `qgis_copilot/tools/raster/dem_validators.py`（新增，DEM 结果语义验证）
- `qgis_copilot/tasks/raster_processing.py`（复用栅格任务调度，不加入计划构建）
- `qgis_copilot/application/controller.py`（仅最小类型化分派）
- `qgis_copilot/ui/raster_plan_card.py`（仅栅格计划展示）
- `tests/test_goal11_dem_analysis.py`（新增）

### 工作内容

- `slope_from_dem`：要求单波段有效 DEM、明确高程/水平单位和 Z factor、明确新输出；
- 计划卡显示输入 DEM、波段、单位、Z factor、算法/provider、输出 CRS/范围、预计影响和风险；
- 确认后异步执行，验证输出可重开、单波段、范围/CRS合理、像元值在坡度算法的预期语义内；
- Desktop 成功验收后才增加 `aspect_from_dem`；坡向通过后再增加 `contour_from_dem`，等高线作为矢量输出使用独立结果验证；
- 不在同一 Goal 混入填洼、流向、流域、阴影或任意栅格计算器。

### 验收

- 用可人工判断的合成小 DEM 验证坡度方向和数值语义；
- 覆盖地理 CRS、单位不明、无 NoData 定义、无效波段、已有输出、取消和 Processing 失败；
- 成功输出加入项目，源 DEM 不变；
- 结果卡能缩放至图层和查看输出路径；
- Desktop 中显示真实 Processing、输出图层和可观察地形结果。

## 6. Goal 12：栅格整理与矢量-栅格组合

### 目标

在 DEM 垂直切片稳定后，补齐分析链真正需要的栅格整理与汇总，而不是扩展全部栅格工具。

### 依次进入

1. `clip_raster_by_mask`：矢量掩膜裁剪栅格；
2. `reproject_raster`：明确目标 CRS、分辨率与重采样方法；
3. `zonal_statistics`：明确栅格波段、分区图层、统计项和字段前缀；
4. 栅格计算器仅作为后续候选，不在本 Goal 开放自由表达式。

每项必须独立通过计划零副作用、结果不变量、取消/失败清理和 Desktop 验收，才能进入下一项。

## 7. Goal 13：受控自动制图

### 进入条件

Goal 10-12 通过；结果图层具有稳定字段/统计结构；先完成可交互原型并由哥哥验收。

### 第一批场景

- 数值字段分级设色；
- 分类字段唯一值符号；
- DEM 坡度/高程的固定色带表达；
- 基于当前地图创建新的 A4 布局并导出 PNG/PDF。

### 边界

- 默认创建命名样式快照或新布局，不覆盖用户现有样式/布局；
- 分类字段、分级方法、级数、色带、标签字段、纸张和 DPI 必须明确；
- 提交前显示样式/布局预览与影响；
- 不能把模型审美判断当作验收，验收检查图层绑定、分类数量、图例、比例尺、标题、范围、字体溢出和导出尺寸；
- 不做“自动把地图变专业”的无界请求。

## 8. Goal 14：可追溯报告生成

### 进入条件

结构化分析结果和布局导出已稳定；先确认报告模板和真实用户流程。

### 第一批交付

- 从本轮已验证的工具结果生成结构化 HTML 报告；
- 报告包含数据范围、参数、统计表、地图、输出路径、限制和审计摘要；
- HTML 验收后再增加 PDF，不能直接用模型自由文本拼 PDF；
- 每个数字和地图必须能追溯到工具结果或已验证输出；
- 数据变化、图层移除或输出缺失时拒绝生成过时报告。

## 9. 高风险独立路线

### 9.1 原地批量编辑

进入前必须先实现：变更集预览、命中数量和样本、活动编辑检测、数据源事务能力检查、撤销/备份策略、修改后复核和二次确认。第一项只允许可逆的单字段常量更新；不与删除同批实现。

### 9.2 删除

使用 `DESTRUCTIVE` 权限和独立确认卡。必须明确图层、要素/字段 ID、数量、样本、备份和不可逆影响。删除永不参与自动执行；未验证撤销或备份前不得进入产品实现。

### 9.3 受限自动执行

只对用户保存并预先授权的固定工作流开放；工作流锁定工具白名单、参数范围、输出目录和失败策略。遇到输入变化、风险升级、覆盖、删除或权限变化立即暂停并要求确认。

### 9.4 任意代码

普通模式始终不注册。未来如做开发者模式，必须与普通工具注册表隔离，明确代码来源、系统权限和审计；不得由模型自行开启，也不得作为实现缺失工具的捷径。

### 9.5 MCP

MCP 仅在出现明确外部数据或互操作任务后立项。先做一个服务器、一个只读工具的最小切片；逐服务器授权、工具白名单、数据预览、超时、取消、断线恢复和审计全部通过后，才考虑写入能力。

## 10. 推荐顺序与第一轮范围

```text
Goal 10 能力治理与栅格基础底座
  -> Goal 11 坡度分析垂直切片，再扩坡向/等高线
  -> Goal 12 栅格整理与分区统计
  -> Goal 13 受控自动制图
  -> Goal 14 可追溯报告
  -> 高风险能力分别立项，不自动接续
```

第一轮只执行 **Goal 10**。不得在同一轮实现坡度、制图、报告、原地编辑、删除、自动执行、任意代码或 MCP。

## 11. 完成定义

单个 Goal 只有在以下条件全部满足时完成：

- 能力卡、契约、权限和不做内容与 PRD 一致；
- 新业务模块和独立逻辑按职责单开文件，既有入口文件仅做最小适配；
- Goal 1-9 的稳定代码没有被无批准重写、重命名、搬迁或删除；
- 目标 QGIS 算法/provider 已真实探测；
- 计划阶段零副作用；
- 确认后执行可取消，失败有清理；
- 结果通过该能力族的内容验证，不只是文件存在；
- 专项测试和 Goal 1-9 固定回归矩阵在独立 QGIS 进程 exit 0；
- 真实 Desktop 同时通过旧代表路径和当前新能力的成功、取消、失败/拒绝路径；
- UI 没有新增无效入口或工具列表堆积；
- README、架构、任务状态、发现记录与实际能力一致；
- 没有敏感数据、临时输出、文件锁或调试残留。
