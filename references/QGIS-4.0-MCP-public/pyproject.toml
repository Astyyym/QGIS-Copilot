[project]
name = "qgis-4.0-mcp-public"
version = "0.2.0"
description = "AI-driven GIS control for QGIS 4.x via MCP TCP socket"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.3",
]

[project.urls]
Repository = "https://github.com/Astyyym/qgis-4.0-mcp-public"
